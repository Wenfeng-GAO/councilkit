{
  "name": "@councilkit/cli",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "CouncilKit CLI — council orchestration outside the browser.",
  "engines": {
    "node": "^22"
  },
  "bin": {
    "councilkit": "./bin/councilkit.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "tsx src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run tests"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "esbuild": "^0.21.5",
    "tsx": "^4.23.1",
    "typescript": "^5.6.2",
    "vitest": "^1.6.0"
  }
}
