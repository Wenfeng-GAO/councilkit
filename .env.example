# CouncilKit 环境变量示例。复制为 .env.local 并按需填写。
# 当前版本所有模型调用都经 Runtime Host 驱动本机 CLI 完成，浏览器与 Host 均不
# 读取任何 API Key；此处不再有 gateway 时代的 VITE_*_API_KEY / base_url 条目。

# 可选：强制 Runtime Host 以 production 模式运行（默认 development）。
# COUNCILKIT_MODE=production
