# 评审解读开发与最终验收

交付代码版本：`f9c5abd83a3f357f3f35bbb71e50a7a6004f40ad`；动态测试候选：`b664eba266929f3f7f28287e5cec91110360cc68`，分支 `hengzhuo/review-explainer`。本报告只记录该候选的验收；后续提交本报告不改变产品代码。

结论：**按开发前冻结的 A01–A12 标准，开发及独立验收全部通过。** [独立结构化回执](2026-09-23-review-explainer-final-receipt.json)。

产品已实现完整冻结 diff 与行内评审、真实模型按需解释和缓存、固定 Canvas 图解与文字回退，以及「打算修复 / 不修复 / 撤回」决定。打算修复限定导出任务和验收断言；不修复在同 PR 后续普通/对照 review 中继承，仅匹配已确认的同一断言。Builder claim 不直接成为已验证关闭。

## 先测试、后实现

1. `cbbf417`：先冻结验收合同、UT/HTTP/CLI/E2E 测试和实际 RED 记录。
2. `fa5b70d`：实现功能，68 条新增自动化用例和 20 条 E2E 通过；独立验收仍发现两项合同违例，未宣称整体通过。
3. `f5497cf`：先补反例，再修复无装饰原断言误跳过新机制、重命名文件旧侧意见消失。
4. `b664eba`：仅把新增真实 CLI 提取用例移至 CLI 的 Node/ES2022 测试域。断言保留；产品源码与 `f5497cf` 完全相同。最终类型检查和 E2E 完整构建通过。

5. `f9c5abd`：仅将冻结 fixture 的一处引号改为 Biome 要求的格式，脚本验证字面量值完全相同。全部 69 个变更代码/测试文件 lint 通过；生产源码及测试语义与动态测试候选一致，无需重跑业务测试或模型。

原标准见 [验收合同](2026-09-23-review-explainer-acceptance.md)，全部更正见 [测试修正审计](2026-09-23-review-explainer-test-amendments.md)。原冻结 hash、失败日志及首轮独立反例保留，没有 skip、重试掩盖或降低业务断言。

## 最终结果

| 标准 | 结果 | 实际验收内容 |
|---|---|---|
| A01 完整 diff | 通过 | 全文件/hunk、无意见改动、增删/改名/二进制/缺失状态、旧新侧及冻结内容 |
| A02 可信定位 | 通过 | 旧侧删除行、重命名旧路径、多位置/上下文、待定位；独立旧路径反例复验通过 |
| A03 简单决定 | 通过 | 默认待决定、两个选择与撤回、行内/解释区同步、键盘 Enter 保存、原文可读 |
| A04 持久化 | 通过 | PR 权威文件、并发冲突、失败不冒充保存、刷新/新浏览器上下文/真实 Host PID 变化后保持 |
| A05 后续跳过 | 通过 | fresh/against 继承、子 Run 撤回覆盖、跨 PR 隔离、确认别名；新机制不被旧决定豁免 |
| A06 修复验收范围 | 通过 | UI→真实导出→候选回执→对照复审输入；只含所选项、原断言/反例/预期不丢；claim 非关闭 |
| A07 真实模型与缓存 | 通过 | 两次真实执行，经生产 API/磁盘缓存/页面；同键去重，失败可重试，重启最终候选后仍复用原结果 |
| A08 解释边界 | 通过 | 证据/推演分开、建议未验证、受限图结构、拒绝可执行输出、Canvas 不可用时文字可读 |
| A09 页面可用性 | 通过 | 1440/678/390 宽度的实际遮挡/坐标断言、图放大后回源码、键盘路径 |
| A10 Host 边界 | 通过 | session/CSRF/Origin、路径穿越、允许文件/体积/损坏工件；测试控制入口未装配到生产 |
| A11 工程检查 | 通过 | 353 条 UT/HTTP/CLI 与相关回归、20 条 E2E、完整 typecheck 和生产 build |
| A12 独立验收 | 通过 | 独立会话核对最终候选、353/20 条结果、两项反例消除、真实模型质量及缓存页面，A01–A12 全部 verified |

自动化：**79 条新增/补充用例 + 274 条相关既有回归 = 353 通过，0 失败、0 跳过**；E2E **20/20 通过，0 flaky、0 skipped，retries=0**。测试通过不意味着整仓所有测试都已运行；这里覆盖冻结功能集合及选定的受影响回归。

352 条主测试的执行期间仅发生测试目录迁移，产品源码始终与最终候选一致；主测试实际执行迁移后的 24 条 identity 用例，移出的 1 条 CLI 集成用例另在最终提交运行通过。最终两份结果共同覆盖全部测试，无断言遗漏。两个同名参数化用例的 candidate ID 不同，属于不同反例。

## 真实模型解释质量

模型：`codex-app-server / gpt-5.6-sol`，只输入两份合成源码和合成评审，无真实 PR 数据。模型原始执行时的候选为 `fa5b70d`；解释服务、prompt、schema、缓存键和 spawn 文件与最终候选内容 hash 完全一致。最终候选以同一临时 home 重启，生产 API 与页面重新读取同一缓存，新增模型调用为 **0**。

- 简单项：解释重复消息的维护问题，明确可选整理；建议仅抽取字符串常量，每次仍新建 Error，没有虚构修复成功。
- 复杂项：解释远端已接收而本地返回 false/无记录；受控测试只证明接受一次且无本地记录，重复执行被明确限定为重试且无幂等保护的推演，生产可达和概率仍未知。
- 图采用固定时序模板并提供等价文字；本次模型未生成逐节点源码链接，原意见仍保留可信源码锚点。图不表示实际运行回放。

执行 ID：`explain-d57ce387-1da3-4a05-a019-17fb084d640e`、`explain-b37f7d43-98c4-46f4-a4e8-8dab246ae94f`。独立验收者逐项对照原始 execution、API payload、缓存与页面文本，不以 JSON 非空代替质量判断。

## 原始证据

本机工件根目录：`/Users/hengzhuo/.codex/artifacts/councilkit-review-explainer-20260923/`。

- `red/`、`test-freeze.v*.json`：测试先行及冻结记录。
- `candidate-identity.json`：最终候选、产品源码等值检查、变更文件内容 hash。
- `tests-final-candidate.json/.log/.exit`、`moved-assertion-b664eba.json/.log/.exit`：353 条实际结果。
- `e2e-b664eba.json/.log/.exit`：最终 20 条 E2E、完整 build；`typecheck-b664eba.log/.exit`、`lint-final.log/.exit`：工程检查。
- `independent-acceptance.md`、`independent-acceptance-final.json`、`independent-*-repro*.json`：独立首轮失败与有界复验。
- `live-1/`：两次真实执行、质量判定、受控源码证据；`live-current/`：最终候选 API/cache 回执、实际页面文本/截图及无新调用核对。

复跑使用 Node 22、隔离临时 home，E2E 端口 43839，单 worker，不复用日常 Host。标准命令见验收合同；新增 CLI 集成文件为 `cli/tests/review-explainer-assertion-identity.test.ts`。

本次完成当前分支开发和隔离验收，没有修改真实 PR #128、发起真实修复、推送/合并代码或切换日常 43127 Host。预览使用合成数据；正式使用需构建并启动此 checkout 的 Host。
