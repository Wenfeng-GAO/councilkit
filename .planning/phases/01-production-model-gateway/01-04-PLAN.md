---
phase: 01-production-model-gateway
plan: 04
plan_id: 01-04
type: execute
wave: 4
depends_on: [01-02]
files_modified:
  - src/stores/discussion.ts
  - src/stores/queries.ts
  - src/components/room/ErrorBanner.tsx
  - src/components/message/MessageBubble.tsx
  - src/components/room/DiscussionStream.tsx
  - src/app/pages/RoomPage.tsx
  - src/lib/round-errors.ts
  - tests/unit/round-errors.test.ts
autonomous: true
requirements: [GW-01]

must_haves:
  truths:
    - "discussion store 含本轮错误状态：`agentErrors: Record<agentId, GatewayError>`、`roundErrorSummary: { fatalGateways: string[]; recoverableKinds: GatewayErrorKind[]; allOfflineNoSummary: boolean } | null`；reset() 清空三者 —— D-09/D-10/D-12 状态层"
    - "runRound 对每个 agent 先查 `gatewayOffline` 集合（本轮已判定致命 offline 的 gatewayId）：若 agent.gatewayId ∈ 集合 → 不调 dispatchStream，直接 setAgentError(agent.id, {kind:'invalid_key', message:'网关已离线'}) + setAgentStatus offline + continue —— D-11 致命扩散避免重复 401"
    - "runRound 对每个 agent 调 dispatchStream 收到 GatewayError 时按 kind 分支：kind==='invalid_key' → 加 gatewayId 进 gatewayOffline 集合（影响后续同 gateway agent）+ setAgentError + setAgentStatus offline-fatal + break chunk loop；其他 4 类 recoverable → setAgentError + setAgentStatus offline-{kind} + break（不扩散）—— D-11"
    - "runRound 末段：若 `allMessages.length === 0`（全 agent offline/无成功发言）→ **跳过 generateSummary**，round 标 completed 无 summary，set roundErrorSummary.allOfflineNoSummary=true；若 allMessages.length > 0 → 走 generateSummary 基于成功发言正常总结 —— D-12"
    - "generateSummary 自身失败（dispatchMessage throw）→ runRound 内 try/catch 兜底，set roundErrorSummary 含 summary 失败信号（warn tone，文案 '总结生成失败：{message}'），不破坏 round 完成状态 —— D-12 容错"
    - "ErrorBanner 组件 3 态文案与 UI-SPEC copywriting §Top-of-room banner 完全一致：≥1 fatal → '网关「{name}」已离线：密钥无效。该网关下 {N} 个 agent 本轮已跳过。' (error tone)；仅 recoverable → '{N} 个 agent 本轮出错（限流 / 上游 / 超时 / 网络），其余 agent 继续。' (warn tone)；allOfflineNoSummary → '本轮无有效发言，未生成总结。' (warn tone)；dismissible × (aria-label='关闭')；role='alert' —— D-10"
    - "MessageBubble 接 `error?: GatewayError` + `gatewayName?: string`，在 message.content 下方渲染 inline error block：header 14px font-semibold + body 12px regular；fatal → 红色调（text-error bg-error/10 border border-error rounded p-2 mt-2 role='status'）；recoverable → 黄色调（text-warn bg-warn/10 border border-warn）；5 类文案与 UI-SPEC copywriting §Inline agent bubble 完全一致 —— D-10"
    - "DiscussionStream 在 messages 列表上方 mount ErrorBanner（透传 roundErrorSummary）；为每个 agent 的 MessageBubble 透传 agentErrors[agent.id] 与 gatewayName —— D-10 集成"
    - "gateway-propagated inline 文案：同 gateway 兄弟 agent 用 UI-SPEC §Inline-bubble 'gateway offline' propagation copy '⚠ 网关已离线' + '网关 {gateway.name} 已被标记离线，本轮跳过该 agent。' —— D-11"
    - "runRound 10s 超时沿用 R7 locked（不改 TIMEOUT_MS）—— D-12；P02 adapter 已透传 kind:'timeout'，本 plan 仅消费"
    - "错误格式化 helper + 状态分类单测全绿（5 类 → 行为映射 + 致命扩散 + 全离线跳总结）"
  artifacts:
    - "src/lib/round-errors.ts — AgentRoundError interface, classifyRoundErrors(errors), formatGatewayOfflineMessage, isFatal kind 判断"
    - "src/stores/discussion.ts — 加 agentErrors/roundErrorSummary state + setAgentError/setRoundErrorSummary/clearRoundErrors，reset 清空"
    - "src/stores/queries.ts — runRound 重构：gatewayOffline Set + per-agent 致命扩散 + chunk.kind 分类 + 全离线跳总结"
    - "src/components/room/ErrorBanner.tsx — 3 态 banner + dismissible + role=alert"
    - "src/components/message/MessageBubble.tsx — error prop + inline block (5 类文案)"
    - "src/components/room/DiscussionStream.tsx — mount ErrorBanner + pass error/gatewayName to bubble"
    - "src/app/pages/RoomPage.tsx — 透传 discussion store 错误状态到 DiscussionStream"
    - "tests/unit/round-errors.test.ts — classifyRoundErrors + formatGatewayOfflineMessage + runRound 关键路径（mock dispatchStream）"
  key_links:
    - "dispatchStream(GatewayError) → runRound kind 分支 → setAgentError + gatewayOffline Set → 后续同 gateway agent skip"
    - "agentErrors[agent.id] → DiscussionStream → MessageBubble error prop"
    - "roundErrorSummary → DiscussionStream → ErrorBanner tone + copy"
---

<objective>
落地网关错误的完整处理链路：扩展 discussion store 的本轮错误状态、重构 runRound 编排（D-09 消费 5 类错误 + D-11 致命扩散 + D-12 全离线跳总结）、新增 ErrorBanner 顶部汇总 + MessageBubble 内联错误块（D-10 双重呈现）、DiscussionStream 集成。

Purpose: SC#4 要求网关错误呈现清晰用户可见消息且受影响 agent 离线而其他 agent 继续。P02 仅搭契约层透传 GatewayError，本 plan 把错误分类落到编排与 UI 呈现。
Output: runRound 错误编排 + ErrorBanner + MessageBubble inline + DiscussionStream 集成 + 行为单测。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-production-model-gateway/01-CONTEXT.md
@.planning/phases/01-production-model-gateway/01-UI-SPEC.md
@.planning/phases/01-production-model-gateway/01-02-PLAN.md
@.planning/phases/01-production-model-gateway/01-03-PLAN.md

@src/stores/discussion.ts
@src/stores/queries.ts
@src/lib/context.ts
@src/lib/summary.ts
@src/services/dispatch.ts
@src/services/gateway-adapters.ts
@src/types/index.ts
@src/components/message/MessageBubble.tsx
@src/components/room/DiscussionStream.tsx
@src/app/pages/RoomPage.tsx
@src/components/shared/StatusPill.tsx
@src/components/agent/AgentConfigCard.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: discussion store 扩展错误状态 + round-errors helper + runRound 重构</name>
  <files>src/lib/round-errors.ts, src/stores/discussion.ts, src/stores/queries.ts, tests/unit/round-errors.test.ts</files>
  <read_first>
    - src/stores/discussion.ts（current stream/drafting/agentStatus/running/lastError + reset/appendMessage/appendDelta/flushDraft/setAgentStatus/setRunning/setError）
    - src/stores/queries.ts（current runRound 含 for-loop agent + try dispatchStream(err→setError+offline+continue) + 末段 generateSummary）
    - src/services/dispatch.ts（P02 dispatchStream(agent, req, onChunk?) AsyncIterable<string|GatewayError>）
    - src/types/index.ts（P02 GatewayError = {kind, message, httpStatus?}, GatewayErrorKind = 5-union）
    - src/lib/summary.ts（generateSummary({gatewayId, model, ...}) 调 dispatchMessage，可能 throw）
    - src/lib/context.ts（buildContext 不变）
    - .planning/phases/01-production-model-gateway/01-CONTEXT.md D-09 (5 类已 contract 落地) D-11 (invalid_key 扩散 gateway / 其他 recoverable 单 agent) D-12 (10s 沿用 / 全离线跳总结 / 部分成功正常总结)
    - .planning/phases/01-production-model-gateway/01-UI-SPEC.md §Error propagation flow (runRound integration 5 步) + §Copywriting Contract §Gateway error presentation（5 类 inline + 3 类 banner + propagation copy）
  </read_first>
  <behavior>
    - Test: classifyRoundErrors([{agentId:'a1', error:{kind:'invalid_key', gatewayName:'Claude'}, {agentId:'a2', error:{kind:'timeout'}}]) → { fatalGateways:['Claude'], recoverableKinds:['timeout'], allOfflineNoSummary:false }
    - Test: classifyRoundErrors([]) → null
    - Test: isFatal({kind:'invalid_key'}) === true；isFatal({kind:'timeout'|'rate_limit'|'upstream'|'network'}) === false
    - Test (mock dispatchStream): runRound with 3 agents g1×2 + g2×1，dispatchStream 对 a1(g1) yield GatewayError{kind:'invalid_key'} → a1 标 offline + agentErrors.a1 set；a2(g1) 不调 dispatchStream，直接 agentErrors.a2 含 'gateway offline' propagated；a3(g2) 正常 yield string → in allMessages
    - Test (mock dispatchStream): runRound with 2 agents，dispatchStream 全 yield GatewayError (a1 timeout + a2 network) → allMessages.length===0，generateSummary 不被调用，round 标 completed 无 summary，roundErrorSummary.allOfflineNoSummary===true
    - Test (mock dispatchStream): runRound 含 1 agent 成功 + 1 失败 → generateSummary 用成功消息生成 summary（正常路径），roundErrorSummary 非 null banner 显示 '1 个 agent 本轮出错…'
    - Test (mock dispatchStream): generateSummary 自身 throw → runRound catch 兜底 setRoundErrorSummary 含 summary 失败 warn 信号，round.completed 不变
  </behavior>
  <action>
    新建 src/lib/round-errors.ts：
    - 导出 `interface AgentRoundError { agentId: string; error: GatewayError; gatewayName?: string }`
    - 导出 `interface RoundErrorSummary { fatalGateways: { gatewayId: string; gatewayName: string; agentCount: number }[]; recoverableCount: number; recoverableKinds: GatewayErrorKind[]; allOfflineNoSummary: boolean; summaryFailed?: { message: string } }`
    - 导出 `function isFatal(error: GatewayError): boolean { return error.kind === 'invalid_key' }`（D-11 致命判定）
    - 导出 `function classifyRoundErrors(agentErrors: AgentRoundError[], allOffline: boolean): RoundErrorSummary | null`：若 agentErrors 空 → null；否则扫描：fatalGateways 收集 invalid_key 错误对应 gatewayId/gatewayName（去重 + 统计该 gateway 受影响 agent 数）；recoverableKinds 收集非 fatal 的 kind；allOfflineNoSummary=allOffline；recoverableCount = 非致命错误数。
    - 导出 `function formatInlineHeader(error: GatewayError): string`：按 kind 返回 5 类 header（与 UI-SPEC §Inline agent bubble 一致）：invalid_key→'⚠ 密钥无效，已离线'、rate_limit→'⚠ 限流，已暂停'、upstream→'⚠ 上游故障'、timeout→'⚠ 请求超时'、network→'⚠ 网络错误'
    - 导出 `function formatInlineBody(error: GatewayError, gatewayName?: string): string`：按 kind 返回 5 类 body（含 gatewayName 占位）—— 文案见 UI-SPEC copywriting 表（密钥被拒绝/触发速率限制429/返回5xx/10s未响应/无法连接到 baseUrl）
    - 导出 `function formatGatewayOfflineInline(): { header: string; body: (gatewayName: string)=>string }`：header='⚠ 网关已离线'，body(gatewayName)=`网关 ${gatewayName} 已被标记离线，本轮跳过该 agent。`（D-11 propagation）

    src/stores/discussion.ts 扩展：
    - state 加 `agentErrors: Record<string, GatewayError>`、`agentErrorGatewayName: Record<string, string|undefined>`、`roundErrorSummary: RoundErrorSummary | null`
    - 方法加 `setAgentError(agentId, error, gatewayName?)`、`setRoundErrorSummary(summary)`、`clearRoundErrors()`、`clearRoundErrorSummary()`
    - `reset()` 增加 `agentErrors:{}, agentErrorGatewayName:{}, roundErrorSummary:null`
    - 保留 lastError 不动（向后兼容）
    - AgentStatus type 扩展：现有 'online'|'offline'|'typing' 加 'offline-fatal'|'offline-recoverable'（或维持 'offline' 单值 + 通过 agentErrors 区分 —— **决定**：维持 'offline' 单值避免大改 AgentStatus 类型 + store 既有用法；UI 通过 agentErrorGatewayName + agentErrors.kind 渲染 dot 颜色 + tooltip。AgentStatus 类型不动）

    src/stores/queries.ts runRound 重构：
    1) `const gatewayOffline = new Set<string>()`（本轮致命 gateway 集合，in-memory）
    2) `store.clearRoundErrors()` 在 reset 之后
    3) for-loop 每 agent：
       - 若 `gatewayOffline.has(agent.gatewayId)` → `store.setAgentStatus(agent.id, 'offline')` + `store.setAgentError(agent.id, {kind:'invalid_key', message:'网关已离线'}, gateway?.name)` + `const off = formatGatewayOfflineInline(); store.appendMessage?(...)` —— 不需创建 system message 更简单：仅 setAgentError + 不调 dispatchStream + continue（D-11 propagated 文案在 MessageBubble render 时通过 round-errors helper formatGatewayOfflineInline 生成）
       - 否则调 `for await (const chunk of dispatchStream(agent, req))`:
         - `if (typeof chunk === 'string') { local += chunk; store.appendDelta(agent.id, chunk); }`
         - `else`（GatewayError）:
           - `store.setAgentError(agent.id, chunk, gateway?.name)`（gateway 名通过 db.gateways.get(agent.gatewayId) 预取，或在 dispatch 前 `const gateway = await db.gateways.get(agent.gatewayId)` 提前 resolve）
           - 若 `isFatal(chunk)` → `gatewayOffline.add(agent.gatewayId)`
           - `store.setAgentStatus(agent.id, 'offline')`
           - `break`（不再消费后续 chunk）
       - try/catch 仍包裹 dispatchStream 整段（防御 throw 路径）—— catch 内 setAgentError(agent.id, {kind:'network', message: err.message}) + offline + continue
       - chunk 循环后：若 local.trim().length > 0 → 创建 message + allMessages.push + addMessage + flushDraft + setAgentStatus online；若 local 为空且无 agentError → 维持现状标 offline（旧逻辑保留）；若有 agentError → 不创建 message（用户在 inline error 看到）
    4) for-loop 后：
       - `const allOffline = allMessages.length === 0`
       - 若 allOffline → **跳过 generateSummary**，`store.setRoundErrorSummary({ fatalGateways:[...], recoverableCount, recoverableKinds, allOfflineNoSummary:true })`，round 已 add 到 db.rounds（status 改 completed 在 finally 后单独 update？—— 现状 createRound 默认 status='active'；本 plan 用 db.rounds.update(round.id, {status:'completed'}) 在 finally 后 update；现状 finally 仅 setRunning(false)，不 update round —— 这是既有 bug 不在本 plan 范围扩大；本 plan 维持现状 not update round status，侧 channel 仅 setSummary 跳过）
       - 若 allMessages.length > 0 → 走 generateSummary，try/catch 兜底：成功 → createSummary + db.summaries.add + setSummary；失败 → store.setRoundErrorSummary({...classify..., summaryFailed:{message: err.message}})（warn tone，banner 加额外行总结失败信息）
       - 在两条路径前调 `const summary_ = classifyRoundErrors(collectedAgentRoundErrors, allOffline)`；collectedAgentRoundErrors 是循环中收集的 AgentRoundError[] 数组；setRoundErrorSummary(summary_) 仅当 summary_ !== null（少错误时不展示 banner，避免空 banner）
    5) 注意：generateSummary 调用基于成功 agent 的 gatewayId+model（取 agents.find(a => a 在 allMessages 对应 sender) 的 gatewayId/model —— 若用 agents[0] 可能其本身 offline，应该取首个成功发言的 agent）。**修改**：generateSummary 入参从 agents[0]?.gatewayId 改为用 allMessages 第一个 senderId 反查 agents 取其 gatewayId/model；若 allMessages 为空则不进 summary 路径（allOffline 已分支）。

    src/lib/summary.ts 不动（P02 已改 generateSummary 签名）。
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/round-errors.test.ts</automated>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/biome check src/stores src/lib/round-errors.ts</automated>
  </verify>
  <acceptance_criteria>
    - classifyRoundErrors 空 → null；含 invalid_key → fatalGateways 含对应 gateway；含 timeout/network → recoverableKinds 含对应 kind
    - isFatal(invalid_key)===true；isFatal(其余 4 类)===false
    - runRound mock：a1(g1) 收到 invalid_key → a2(g1) 不调 dispatchStream + agentError 含 propagation；a3(g2) 正常
    - runRound mock：全 offline → generateSummary 不被调（mock 验证未 invoke）+ roundErrorSummary.allOfflineNoSummary===true
    - runRound mock：1 成功 + 1 失败 → generateSummary 用成功消息（mock 验证 invoke with successful agent's gatewayId+model）
    - runRound mock：generateSummary throw → catch + setRoundErrorSummary 含 summaryFailed，round 完成不抛错
    - discussion store reset 清空 agentErrors/agentErrorGatewayName/roundErrorSummary
    - tsc 0 错，biome 通过
  </acceptance_criteria>
  <done>runRound 错误编排 + 致命扩散 + 全离线跳总结 + helper 分类 + 单测全绿；行为完全契合 D-09/D-11/D-12</done>
</task>

<task type="auto">
  <name>Task 2: ErrorBanner + MessageBubble inline + DiscussionStream 集成 + RoomPage 透传</name>
  <files>src/components/room/ErrorBanner.tsx, src/components/message/MessageBubble.tsx, src/components/room/DiscussionStream.tsx, src/app/pages/RoomPage.tsx</files>
  <read_first>
    - src/components/message/MessageBubble.tsx（current 含 message.content + ReactMarkdown + avatar + role + timestamp）
    - src/components/room/DiscussionStream.tsx（current 含 messages.map MessageBubble + draftEntries + 旧 '部分 agent 离线，已跳过' 行内文本要删除替换为 ErrorBanner）
    - src/app/pages/RoomPage.tsx（current 用 DiscussionStream + 含 `lastError ? <p>错误...</p> : null` 旧单错误展示要保留或替换）
    - src/stores/discussion.ts（Task 1 产出 agentErrors/agentErrorGatewayName/roundErrorSummary + setRoundErrorSummary）
    - src/lib/round-errors.ts（Task 1 产出 formatInlineHeader/formatInlineBody/formatGatewayOfflineInline/classifyRoundErrors）
    - src/components/shared/StatusPill.tsx（P03 产出，tone + text）
    - .planning/phases/01-production-model-gateway/01-UI-SPEC.md §Copywriting Contract §Gateway error presentation（5 类 inline header+body + 3 类 banner + propagation + Agent status dot 颜色映射）+ §Interaction Flows §Error propagation flow (5 步) + §States Checklist ErrorBanner 5 态
  </read_first>
  <action>
    新建 src/components/room/ErrorBanner.tsx：props `{ summary: RoundErrorSummary | null; onDismiss: ()=>void }` 内部 visible state（init true，onDismiss 设 false，summary 变化重置 true via useEffect 依赖 summary）。若 !summary || !visible → return null。根据 summary 选 tone + 文案：
    - summary.allOfflineNoSummary===true → warn tone，文案 '本轮无有效发言，未生成总结。'（含或不含 propagation 行可叠加，但 UI-SPEC 此态仅此行）
    - summary.summaryFailed → warn tone，文案 `总结生成失败：${summary.summaryFailed.message}`（追加展示）
    - summary.fatalGateways.length > 0 → error tone，对每个 fatal gateway 渲染一行：`网关「${gw.gatewayName}」已离线：密钥无效。该网关下 ${gw.agentCount} 个 agent 本轮已跳过。`（多 gateway 多行）
    - summary.recoverableCount > 0 → warn tone（与 fatal 并存时合并：fatal 行 error + 单独追加 recoverable warn 行 `${count} 个 agent 本轮出错（限流 / 上游 / 超时 / 网络），其余 agent 继续。`）
    渲染：div role='alert' className `border rounded p-3 text-sm flex items-start justify-between gap-2` + tone class（error:`border-error bg-error/10 text-fg`，warn:`border-warn bg-warn/10 text-fg`）。文案区 <div className="flex flex-col gap-1">。右侧 <button type='button' aria-label='关闭' onClick={onDismiss} className='text-muted hover:text-fg shrink-0'>×</button>。

    src/components/message/MessageBubble.tsx 扩展：
    - props 加 `error?: GatewayError` 与 `gatewayName?: string`（可选）
    - import formatInlineHeader, formatInlineBody, formatGatewayOfflineInline from '@/lib/round-errors'
    - 在 message.content（ReactMarkdown）下方加逻辑：若 error prop 存在 → 渲染 inline block。**注入判断**：若 error.kind==='invalid_key' 且 error.message === '网关已离线'（gateway-propagated sentinel）→ 用 formatGatewayOfflineInline()；否则用 formatInlineHeader(error)/formatInlineBody(error, gatewayName)。Inline block className：fatal (invalid_key 且非 propagation) → `mt-2 rounded border border-error bg-error/10 p-2`；propagation → 同 fatal（红）；recoverable → `mt-2 rounded border border-warn bg-warn/10 p-2`。结构：`<div role="status" className="ml-8 ...">` + `<p className="text-sm font-semibold">${header}</p>` + `<p className="text-xs leading-snug">${body}</p>`
    - 保留现有 message 渲染不变

    src/components/room/DiscussionStream.tsx 重构：
    - 从 useDiscussionStore 读 `agentErrors`, `agentErrorGatewayName`, `roundErrorSummary`
    - 顶部 mount `<ErrorBanner summary={roundErrorSummary} onDismiss={()=>store.clearRoundErrorSummary()} />`（clearRoundErrorSummary 设为 null 即隐藏）
    - messages.map 传 `{message, agent, error: agentErrors[m.senderId], gatewayName: ...}`（仅对 senderType==='agent' 且 m.senderId 在 agentErrors 中传 error）
    - 删除旧 '部分 agent 离线，已跳过' 那行 <p>，由 ErrorBanner 取代
    - draftEntries 不需传 error（draft 状态不展示 error；error 来自完成 agent）

    src/app/pages/RoomPage.tsx 调整：
    - 删除旧 `lastError ? <p ...>错误: {lastError}</p> : null`（由 ErrorBanner 取代，避免重复展示）
    - 不需透传新 props（DiscussionStream 直接读 store）
    - 保留 running/startRound/summary 流程不变

    无新单测（UI wiring 验证靠 tsc + biome + P05 浏览器手测；ErrorBanner 文案字面对照 UI-SPEC copywriting）。
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/biome check src/components/room/ErrorBanner.tsx src/components/message/MessageBubble.tsx src/components/room/DiscussionStream.tsx src/app/pages/RoomPage.tsx</automated>
    <automated>rg -c 'role="alert"' src/components/room/ErrorBanner.tsx | grep -q 1 && rg -c 'role="status"' src/components/message/MessageBubble.tsx | grep -q 1 && echo "aria roles present"</automated>
  </verify>
  <acceptance_criteria>
    - ErrorBanner 3 态文案字面与 UI-SPEC §Top-of-room banner 表完全一致；含 dismissible × + aria-label='关闭' + role='alert'
    - MessageBubble 含 error prop 时在 content 下方渲染 inline block；header 14px font-semibold；body 12px regular；fatal 红 / recoverable 黄；role='status'
    - MessageBubble 5 类 inline header 字面与 UI-SPEC §Inline agent bubble 表一致（含 ⚠ 前缀）
    - DiscussionStream 顶部 mount ErrorBanner；每个 agent MessageBubble 接收 agentErrors[id]；旧 '部分 agent 离线' 行删除
    - RoomPage 删除旧 lastError 单行展示
    - tsc 0 错，biome 通过
  </acceptance_criteria>
  <done>错误呈现组件全部按 UI-SPEC 落地，runRound 错误状态经 DiscussionStream/MessageBubble/ErrorBanner 双重呈现，aria 角色正确</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GatewayError → UI | 上游错误消息跨此边界进入 innerText；非可信输入需脱敏 |
| discussion store → React render | agentErrors/roundErrorSummary 跨此边界驱动 UI 渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | 上游 errorMessage 透传到 MessageBubble inline body | medium | mitigate | formatInlineBody 不直接注入 chunk.message 原文 —— 使用预定义中英文模板（按 kind），仅插入 gatewayName（用户自配）与 baseUrl（gateway.baseUrl，本地数据）。GatewayError.message 仅在 console 用于调试，不展示给用户。UI-SPEC copywriting 5 类 body 文案统一受控 |
| T-04-02 | Information Disclosure | ErrorBanner 显示 N agent 个数 + gateway 名泄露配置 | low | accept | 本地单用户工具，gateway name 是用户自命名，N 是统计值；不构成敏感信息泄露 |
| T-04-03 | Denial of Service | 致命 gateway 导致全 round 跳过，用户反复点开始新一轮触发重复 401 | low | mitigate | D-11 gatewayOffline Set 本轮内短路同 gateway agent，已避免重复请求；用户主动新一轮会重置（runRound reset 清 set）；不引入跨 round 节流避免过度设计 |
| T-04-04 | Tampering | roundErrorSummary state 跨组件共享可被意外 mutate | low | mitigate | Zustand set 模式不可变更新；setRoundErrorSummary 接受新对象 not mutate；clearRoundErrorSummary 设 null |
| T-04-05 | Information Disclosure | generateSummary 失败 message 显示 '总结生成失败：{err.message}' | low | mitigate | err.message 仅短描述（如 'HTTP 429'），不含上游响应体；UI-SPEC banner warn tone 不刷屏 |
| T-04-SC | Tampering | 无 npm 安装 | low | accept | 无新依赖 |
</threat_model>

<verification>
- ./node_modules/.bin/vitest run tests/unit/round-errors.test.ts 全绿
- ./node_modules/.bin/tsc --noEmit 0 错
- ./node_modules/.bin/biome check src 0 错
- 浏览器手测（P05 实跑）：填错 key 看 invalid_key 红 inline + banner；填对 key 后 引发 timeout/网络 看 黄 inline + warn banner；全部 agent 失败 看 'allOffline' banner 无 summary
</verification>

<success_criteria>
- runRound 消费 5 类 GatewayError，按 kind 分支（fatal 扩散 / recoverable 单 agent）（D-09/D-11）
- 全 agent offline 跳过 generateSummary（D-12）
- 部分成功保留 generateSummary 基于成功发言（D-12）
- ErrorBanner 顶部 banner + MessageBubble inline error 双重呈现（D-10）
- 5 类 inline 文案 + 3 类 banner 文案 + propagation 文案字面与 UI-SPEC 一致
- 10s timeout 沿用（R7/D-12 不改）
- aria 角色 alert/status 正确（d11 a11y）
- 全部 typecheck/lint/unit 绿
</success_criteria>

<output>
Create `.planning/phases/01-production-model-gateway/01-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- **Error helpers** (src/lib/round-errors.ts): `AgentRoundError`, `RoundErrorSummary`, `isFatal`, `classifyRoundErrors`, `formatInlineHeader`, `formatInlineBody`, `formatGatewayOfflineInline`
- **Discussion store** (src/stores/discussion.ts): 新 state `agentErrors`, `agentErrorGatewayName`, `roundErrorSummary` + setters；reset 清空
- **Round orchestration** (src/stores/queries.ts): runRound 重构 — gatewayOffline Set + per-agent 致命扩散 + chunk.kind 分类 + 全离线跳总结 + summary throw 兜底
- **ErrorBanner** (src/components/room/ErrorBanner.tsx): 3 态 + dismissible + role=alert
- **MessageBubble** (src/components/message/MessageBubble.tsx): error + gatewayName props + inline 5 类文案 block + role=status
- **DiscussionStream** (src/components/room/DiscussionStream.tsx): mount ErrorBanner + pass error to bubble + 删除旧 offline 行
- **RoomPage** (src/app/pages/RoomPage.tsx): 删除旧 lastError 单行展示
- **Tests** (tests/unit/round-errors.test.ts): classifyRoundErrors + isFatal + 4 runRound 关键路径 (致命扩散/全离线/部分成功/summary 失败)
