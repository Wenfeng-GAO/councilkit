---
phase: 01-production-model-gateway
plan: 01
plan_id: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/gateway.ts
  - src/models/index.ts
  - src/lib/db.ts
  - src/lib/crypto.ts
  - src/lib/gateway-migrate.ts
  - tests/unit/gateway-crypto-migrate.test.ts
autonomous: true
requirements: [GW-01]

must_haves:
  truths:
    - "Dexie `gateways` 表存在，schema 字段 {id, name, type('anthropic'|'openai-compatible'), baseUrl, defaultModel, createdAt}；可添加任意数量命名 gateway（D-01）"
    - "crypto.ts 提供 per-gatewayId AES 加密：saveGatewayApiKey(gatewayId, plain) / loadGatewayApiKey(gatewayId) / clearGatewayApiKey(gatewayId)；localStorage key 形如 councilkit.gateways.{id}.enc；沿用固定 passphrase 'councilkit-local-v1'（D-06, D-07）"
    - "gateway 元数据（name/type/baseUrl/defaultModel）只在 Dexie `gateways` 表；apiKey 只在 localStorage AES cipher，两者绝不互存（D-07）"
    - "启动迁移 migrateLegacyAgentsToGateways：检测 agent.model ∈ {'claude','openai','deepseek'} 时 seed 三个占位 gateway（名称 Claude/OpenAI/DeepSeek；type anthropic/openai-compatible/openai-compatible；baseUrl 用官方默认 https://api.anthropic.com / https://api.openai.com/v1 / https://api.deepseek.com/v1；**无 key**），回填 agent.gatewayId，丢弃旧 model 标签（D-03）。迁移幂等：第二次启动不再重复 seed"
    - "createGateway/validateGateway 单测 + 多 key crypto 单测 + 迁移单测全部通过"
  artifacts:
    - "src/models/gateway.ts — Gateway interface, createGateway, validateGateway"
    - "src/lib/db.ts — CouncilKitDB v2 schema（保留 v1 兼容）+ addGateway/getGateway/listGateways/deleteGateway/updateGateway helpers"
    - "src/lib/crypto.ts — saveGatewayApiKey/loadGatewayApiKey/clearGatewayApiKey（保留旧 saveApiKey/loadApiKey 仅供迁移读取）"
    - "src/lib/gateway-migrate.ts — migrateLegacyAgentsToGateways(db)"
    - "tests/unit/gateway-crypto-migrate.test.ts"
  key_links:
    - "gateway.id ←→ localStorage cipher key 'councilkit.gateways.{id}.enc'"
    - "gateway.id ←→ agent.gatewayId 引用（agent 字段在 P02 落地，本 plan 仅提供 gateway 表与 id 生成）"
---

<objective>
建立 Production Model Gateway 的数据基座：通用 gateway 列表数据模型（D-01）、Dexie `gateways` 表、固定 passphrase 多 key AES 加密（D-06/D-07）、以及旧 `agent.model` 标签数据到占位 gateway 的启动迁移（D-03）。

Purpose: 后续 plan（agent 字段改造、adapter 分派、settings 页）依赖一个可持久化、可加密存 key 的 gateway 实体；本 plan 落地该实体与数据迁移路径，使旧数据在新 schema 下不丢失。
Output: gateway model + Dexie v2 schema + 多 key crypto + 启动迁移函数 + 单测。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-production-model-gateway/01-CONTEXT.md

@src/lib/db.ts
@src/lib/crypto.ts
@src/models/agent.ts
@src/models/index.ts
@src/models/template.ts
@tests/unit/models.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 Gateway model + Dexie gateways 表 + helpers</name>
  <files>src/models/gateway.ts, src/models/index.ts, src/lib/db.ts, tests/unit/gateway-crypto-migrate.test.ts</files>
  <read_first>
    - src/models/agent.ts（沿用 validateXxx 返回 ValidationResult 的模式）
    - src/models/template.ts（沿用 createXxx 抛错的工厂模式）
    - src/models/index.ts（re-export 模式）
    - src/lib/db.ts（current CouncilKitDB v1 schema + addRoom/getRoom helpers 模式）
    - .planning/phases/01-production-model-gateway/01-CONTEXT.md（D-01 字段定义、D-07 Dexie 表）
  </read_first>
  <behavior>
    - Test: createGateway({name:'Claude 主账号', type:'anthropic', baseUrl:'https://api.anthropic.com', defaultModel:'claude-sonnet-4'}) 返回带 id/createdAt 的对象且 validateGateway 通过
    - Test: createGateway 抛错当 name 为空 或 type 不在 {'anthropic','openai-compatible'} 或 baseUrl 为空 或 defaultModel 为空
    - Test: createGateway 抛错当 baseUrl 不是 http(s):// 开头
    - Test: CouncilKitDB v2 schema 含 'gateways' 表且 index 含 'id, type'
  </behavior>
  <action>
    新建 src/models/gateway.ts：导出 `GatewayType = 'anthropic' | 'openai-compatible'` 类型与 `Gateway` interface（字段 id:name `string`、name、type: `GatewayType`、baseUrl、defaultModel、createdAt: `number`）。实现 `validateGateway(g): ValidationResult` —— 检查 name 非空、name 长度 ≤ 50、type ∈ 集合、baseUrl 匹配 `^https?://` 、defaultModel 非空。实现 `createGateway(input: {name, type, baseUrl, defaultModel}): Gateway` —— id = `crypto.randomUUID()`，createdAt = `Date.now()`，调用 assertValid（沿用 src/models/index.ts 中 assertValid 模式）。在 src/models/index.ts 中 re-export `Gateway`, `GatewayType`, `createGateway`, `validateGateway`，并在 export type 行加上 Gateway。

    扩展 src/lib/db.ts：CouncilKitDB 增加 `gateways!: Table<Gateway, string>`；在 constructor 中新增 `this.version(2).stores({ rooms:'id, status, lastActiveAt', agents:'id, roomId, model', messages:'id, roundId, senderId', rounds:'id, roomId, roundNumber', summaries:'id, roundId', gateways:'id, type' })` —— **保留 v1 stores 字段以兼容已存在数据库**（Dexie 要求 v2 含 v1 全部 stores）。新增 helpers：`addGateway(gateway)`、`getGateway(id)`、`listGateways()`（按 createdAt 升序）、`updateGateway(id, changes)`、`deleteGateway(id)`。导入 Gateway 类型从 `@/models`。
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/gateway-crypto-migrate.test.ts -t 'createGateway'</automated>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/biome check src/models/gateway.ts src/lib/db.ts</automated>
  </verify>
  <acceptance_criteria>
    - createGateway 产出对象含 id（uuid 形）+ createdAt（数值），且 validateGateway 返回 {ok:true}
    - validateGateway 对空 name / 错误 type / 非 http(s) baseUrl / 空 defaultModel 均返回 {ok:false} 且 errors 非空
    -CouncilKitDB 实例化后 db.gateways 表存在（通过 db.gateways.add / get 验证）
    - tsc --noEmit 0 错；biome check 通过
  </acceptance_criteria>
  <done>Gateway model + Dexie v2 gateways 表 + 5 个 helpers 落地，单测通过，typecheck/lint 绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 crypto.ts 为多 key（per-gatewayId AES）</name>
  <files>src/lib/crypto.ts, tests/unit/gateway-crypto-migrate.test.ts</files>
  <read_first>
    - src/lib/crypto.ts（current 单 key 实现：STORAGE_KEY='councilkit.key.enc', PASSPHRASE='councilkit-local-v1', saveApiKey/loadApiKey/clearApiKey/encryptApiKey/decryptApiKey）
    - .planning/phases/01-production-model-gateway/01-CONTEXT.md（D-06 多 key 固定 passphrase、D-07 apiKey 仅 localStorage、Claude's Discretion 中 localStorage key 命名 'councilkit.gateways.{id}.enc'）
  </read_first>
  <behavior>
    - Test: saveGatewayApiKey('g1', 'sk-abc') 写入 localStorage['councilkit.gateways.g1.enc'] 为非空 cipher（不等于明文 'sk-abc'）
    - Test: loadGatewayApiKey('g1') === 'sk-abc'
    - Test: loadGatewayApiKey('nope') === null（未存）
    - Test: clearGatewayApiKey('g1') 后 loadGatewayApiKey('g1') === null
    - Test: 不同 gatewayId 互不干扰（g1 的 key 不影响 g2）
    - Test: 旧 saveApiKey/loadApiKey 仍可读写 localStorage['councilkit.key.enc']（迁移期保留，不删）
  </behavior>
  <action>
    在 src/lib/crypto.ts 新增 `gatewayKeyStorageId(gatewayId: string): string` 返回字符串拼接形式 `councilkit.gateways.${gatewayId}.enc`（不要写成模板字面量直接拼 localStorage 调用，函数内构造即可）。新导出 `saveGatewayApiKey(gatewayId, plain)`：localStorage.setItem(gatewayKeyStorageId(gatewayId), encryptApiKey(plain))。新导出 `loadGatewayApiKey(gatewayId): string | null`：从 localStorage 取 cipher，无则 return null；try decryptApiKey，catch 返回 null。新导出 `clearGatewayApiKey(gatewayId): void`：localStorage.removeItem(gatewayKeyStorageId(gatewayId))。保留 encryptApiKey/decryptApiKey 不变；保留 saveApiKey/loadApiKey/clearApiKey 旧导出（迁移函数 Task 3 仍要读 'councilkit.key.enc' 以兼容历史 dev env 配置）。PASSPHRASE 'councilkit-local-v1' 沿用，**不引入** master passphrase（D-06 明确）。
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/gateway-crypto-migrate.test.ts -t 'crypto'</automated>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - saveGatewayApiKey 写入的 localStorage 值不等于明文，含iv等 AES 输出特征
    - loadGatewayApiKey 正确还原明文；缺失返回 null；corrupt cipher 返回 null（不抛错）
    - clearGatewayApiKey 后 loadGatewayApiKey 为 null
    - 两个不同 gatewayId 的 cipher key 互不覆盖
    - 旧 saveApiKey/loadApiKey 仍工作（不破坏 P03 迁移读取）
  </acceptance_criteria>
  <done>多 key crypto 函数落地，与旧单 key API 共存，单测全绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 启动迁移 migrateLegacyAgentsToGateways（D-03 旧 model 标签 → 占位 gateway）</name>
  <files>src/lib/gateway-migrate.ts, tests/unit/gateway-crypto-migrate.test.ts</files>
  <read_first>
    - src/lib/db.ts（db.agents, db.gateways 表 API）
    - src/models/gateway.ts（Task 1 产出的 createGateway）
    - src/models/agent.ts（current Agent.model: ModelType = 'claude'|'openai'|'deepseek'）
    - .planning/phases/01-production-model-gateway/01-CONTEXT.md（D-03 seed 三个占位 gateway 名称/type/官方 baseUrl；占位无 key；幂等）
  </read_first>
  <behavior>
    - Test: 给定 db.agents 中有 {model:'claude'} 和 {model:'deepseek'} 两条记录，调用 migrateLegacyAgentsToGateways 后，db.gateways 含 Claude/DeepSeek 两条占位（gpt 占位只有当存在 'openai' 标签 agent 时才 seed —— 实现可三者全 seed 或按需，二选一取「按需 seed 仅含的标签」，单测覆盖两种标签）
    - Test: 迁移后 agent.gatewayId 非空，agent.model 被改为对应真实 model id 字符串（claude→'claude-sonnet-4' 占位 defaultModel；deepseek→'deepseek-chat'；openai→'gpt-4o'）—— 注：本 plan agent 字段尚未引入 gatewayId，迁移用临时侧通道（见 action 说明），P02 再改 agent 类型
    - Test: 迁移幂等 —— 第二次调用不再 seed 新 gateway（gateways 数量不变），且不重复修改已迁移 agent
    - Test: 已含 'claude-sonnet-4' 等真实 model id（非旧标签）的 agent 不被处理
  </behavior>
  <action>
    新建 src/lib/gateway-migrate.ts 导出 `migrateLegacyAgentsToGateways(): Promise<{ seeded: number; migrated: number }>`。流程：(1) 用 db.agents.toArray() 取全部 agent；(2) 收集出现的旧标签集合 `const legacyTags = new Set(agent.model for agent where model ∈ {'claude','openai','deepseek'})`；(3) 若 'claude' ∈ tags → createGateway({name:'Claude', type:'anthropic', baseUrl:'https://api.anthropic.com', defaultModel:'claude-sonnet-4'}) 写入 db.gateways，记下 id 映射 tag→{id, defaultModel}；'openai' → {name:'OpenAI', type:'openai-compatible', baseUrl:'https://api.openai.com/v1', defaultModel:'gpt-4o'}；'deepseek' → {name:'DeepSeek', type:'openai-compatible', baseUrl:'https://api.deepseek.com/v1', defaultModel:'deepseek-chat'}。(4) 对每个旧标签 agent：将 agent.model 改为对应 defaultModel 字符串（claude→'claude-sonnet-4' 等），并暂存 gatewayId 到 agent 上 **仅当字段已存在**（本 plan Agent interface 尚未含 gatewayId 字段 —— 迁移函数先用 `(agent as Record<string, unknown>).gatewayId = id` 侧通道写入并 db.agents.put 更新；P02 会在 Agent interface 正式加字段后无需这个 cast）。(5) 若无任何旧标签，return {seeded:0, migrated:0} 不写库。

    幂等保证：迁移函数开头扫描 db.gateways 是否已存在 name ∈ {'Claude','OpenAI','DeepSeek'}（按 name 唯一性判断，不靠 id），已存在则复用其 id 不重复 seed。

    在 src/lib/db.ts 文件末尾或新 file 不修改；迁移函数由调用方在 app 启动时触发（本 plan 不接 main.tsx —— P05 验证时由执行方决定接入点；但提供 helper `runStartupMigration()` 内调 migrateLegacyAgentsToGateways， swallowing 错误并 console.warn）。
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/gateway-crypto-migrate.test.ts -t 'migrate'</automated>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/biome check src/lib/gateway-migrate.ts</automated>
  </verify>
  <acceptance_criteria>
    - 含 'claude' 与 'deepseek' 标签的 agent 库 → 迁移后 db.gateways 至少 2 条占位（type 分别 anthropic / openai-compatible），无 key 写入 localStorage（loadGatewayApiKey of 占位 id === null）
    - 迁移后旧标签 agent 的 model 字段被替换为真实 model id 字符串（'claude-sonnet-4' / 'deepseek-chat' / 'gpt-4o'）
    - 重复调用迁移：db.gateways 数量不变、agent 不被重复修改
    - 已含真实 model id 的 agent 不被触碰
    - corrupt 数据 / 空 db 不抛错
  </acceptance_criteria>
  <done>迁移函数 + 启动 helper 落地，D-03 占位 seed + 回填 + 幂等全部由单测覆盖</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage ↔ page | AES cipher 跨此边界持久化；XSS 可读 cipher、不可直接读明文（需 passphrase + crypto-js 解密） |
| Dexie (IndexedDB) ↔ page | gateway 元数据经此边界；不含 key，泄露影响仅限 endpoint 暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | localStorage AES cipher | medium | accept | 本地单用户工具，固定 passphrase 'councilkit-local-v1' 是 TECH 安全边界确认的「混淆非真 secrecy」；Cipher 不进入 git/网络。在 P03 settings 页掩码显示 key，不在 console 打印 cipher。 |
| T-01-02 | Tampering | migrateLegacyAgentsToGateways 写 agent 字段 | low | mitigate | 迁移函数幂等 + 仅处理旧标签集合；对已含真实 model id 的 agent no-op；单测覆盖重复调用不重复修改 |
| T-01-03 | Information Disclosure | 占位 gateway 默认 baseUrl | low | accept | baseUrl 为公开官方端点（api.anthropic.com 等），不构成敏感信息 |
| T-01-SC | Tampering | 无包安装（crypto-js 已在 package.json v4.2.0） | low | accept | 不引入新依赖；既有 crypto-js 版本固定，无需 slopcheck |
</threat_model>

<verification>
- ./node_modules/.bin/vitest run tests/unit/gateway-crypto-migrate.test.ts 全绿
- ./node_modules/.bin/tsc --noEmit 0 错
- ./node_modules/.bin/biome check src 全绿
- 手动：在浏览器 DevTools Application → IndexedDB → councilkit → gateways 表存在；localStorage 中无明文 key
</verification>

<success_criteria>
- gateway 数据模型 + Dexie v2 表 + 5 helpers 可用
- 多 key crypto 按 gatewayId 工作，旧 API 兼容
- D-03 旧标签迁移幂等且 seed 占位无 key
- 所有单测/typecheck/lint 绿
</success_criteria>

<output>
Create `.planning/phases/01-production-model-gateway/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- **Types** (src/models/gateway.ts): `GatewayType`, `Gateway` interface
- **Factories/Validators** (src/models/gateway.ts): `createGateway`, `validateGateway`
- **DB layer** (src/lib/db.ts): `CouncilKitDB` v2 schema 含 `gateways` 表；helpers `addGateway`, `getGateway`, `listGateways`, `updateGateway`, `deleteGateway`
- **Crypto layer** (src/lib/crypto.ts): `saveGatewayApiKey(gatewayId, plain)`, `loadGatewayApiKey(gatewayId)`, `clearGatewayApiKey(gatewayId)`, `gatewayKeyStorageId(gatewayId)`（保留旧 `saveApiKey`/`loadApiKey`/`clearApiKey`/`encryptApiKey`/`decryptApiKey`）
- **Migration** (src/lib/gateway-migrate.ts): `migrateLegacyAgentsToGateways()`, `runStartupMigration()`
- **Tests** (tests/unit/gateway-crypto-migrate.test.ts): gateway validation / multi-key crypto / migration idempotency
