---
phase: 01-production-model-gateway
plan: 03
plan_id: 01-03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/styles/globals.css
  - tailwind.config.ts
  - src/components/shared/StatusPill.tsx
  - src/lib/gateway-test.ts
  - src/components/gateway/GatewayCard.tsx
  - src/components/gateway/GatewayFormModal.tsx
  - src/components/gateway/TestConnectionButton.tsx
  - src/components/gateway/DeleteGatewayModal.tsx
  - src/app/pages/SettingsPage.tsx
  - src/components/layout/Sidebar.tsx
  - src/app/router.tsx
  - src/components/agent/AgentConfigCard.tsx
  - src/app/pages/NewRoomPage.tsx
  - src/stores/gateways.ts
  - tests/unit/settings-store.test.ts
autonomous: true
requirements: [GW-01]

must_haves:
  truths:
    - "sidebar 含第 4 项「设置」NavLink 指向 /settings，置于底部（border-edge 分隔，区别于 主页/新建房间/模板）—— D-05"
    - "router 含 `<Route path='/settings' element={<SettingsPage/>} />` 挂载 AppShell —— D-05"
    - "globals.css 新增 `--color-success:#22c55e` `--color-warn:#d29922` `--color-error:#f85149` `--color-info:#58a6ff` `--color-surface-2:#1e2128`；tailwind.config.ts extend.colors 映射 success/warn/error/info/surface-2 → var() —— 与 P02 错误呈现共用，与 Phase 2 VERIFY-01 协调不发散"
    - "SettingsPage 含 gateway 列表（GatewayCard × N）+ 顶部「+ 添加网关」CTA + 空态（EmptyState）+ GatewayFormModal（增/改）+ DeleteGatewayModal（删）+ TestConnectionButton —— D-05/D-07/D-08"
    - "GatewayFormModal 字段：name / type(select anthropic|openai-compatible) / baseUrl / apiKey(password 掩码) / defaultModel；编辑态 apiKey 显示 ••• 占位，留空则保留既有 cipher —— D-07"
    - "TestConnectionButton 4 态：idle('测试连接')/testing('测试中…' disabled)/success('已连接' success tint disabled)/failed-fatal('重试测试'+密钥无效)/failed-other('重试测试'+连接失败)；点击发 anthropicAdapter/openaiCompatibleAdapter 最小流式请求（model=gateway.defaultModel, max_tokens=1, stream:true）—— D-08"
    - "删除 gateway 走 DeleteGatewayModal 确认（focus 默认到「取消」），确认后 deleteGateway(id) + clearGatewayApiKey(id)；提示该 gateway 下 N agent 的 gatewayId 会被清空（N 由 db.agents.where('roomId')... 实际本 plan 不实施清空 agent 字段，仅文案如实告知，agent 字段清理延后；P05 不动 agent，留作示警）"
    - "NewRoomPage 「添加 agent」Modal：model Select 替换为「网关」Select（options=gateways.name）+ 「模型 ID」TextInput（默认填 selectedGateway.defaultModel）；无 gateway 时显示 warn 通知块 + 「前往设置」链接 —— UI-SPEC copywriting"
    - "AgentConfigCard subtitle 改为 `${gateway.name} · ${agent.model}`（gateway 通过 prop 传入或组件内 lookup）"
    - "API key 在 UI 中任何状态下不显示明文（编辑态用 •••••• 占位）—— 安全约束"
  artifacts:
    - "src/styles/globals.css — 5 个新 CSS 变量"
    - "tailwind.config.ts — 5 个新 color 映射"
    - "src/components/shared/StatusPill.tsx — tone (muted/info/success/error/warn) + text 通用 pill"
    - "src/lib/gateway-test.ts — testGatewayConnection(gateway): Promise<{ok:true}|{ok:false, error:GatewayError}>"
    - "src/components/gateway/GatewayCard.tsx — 列表项 + 3 actions"
    - "src/components/gateway/GatewayFormModal.tsx — 增/改 Modal"
    - "src/components/gateway/TestConnectionButton.tsx — 4 态按钮"
    - "src/components/gateway/DeleteGatewayModal.tsx — 确认 Modal"
    - "src/app/pages/SettingsPage.tsx — 列表 + 空态 + Modal orchestration"
    - "src/stores/gateways.ts — useGateways query hook + CRUD mutations"
    - "src/components/layout/Sidebar.tsx — footer 设置 NavLink"
    - "src/app/router.tsx — /settings route"
    - "src/components/agent/AgentConfigCard.tsx — subtitle gatewayName · model"
    - "src/app/pages/NewRoomPage.tsx — gateway Select + empty notice"
    - "tests/unit/settings-store.test.ts — useGateways query/mutations 行为"
  key_links:
    - "SettingsPage ←→ useGateways (TanStack Query) ←→ db.gateways"
    - "GatewayFormModal onSave → saveGatewayApiKey(id, plain) (localStorage) + addGateway/updateGateway (Dexie)"
    - "TestConnectionButton onClick → testGatewayConnection → anthropicAdapter/openaiCompatibleAdapter (P02)"
    - "Sidebar 设置 NavLink → /settings → SettingsPage (router)"
    - "NewRoomPage gateway Select options ← useGateways list"
---

<objective>
落地用户可见的网关配置入口：扩展设计 token（globals.css + tailwind 5 色映射）、新增 /settings 页面承载 gateway CRUD 与「测试连接」(D-08)，sidebar 第 4 项「设置」入口 (D-05)，router 挂载，以及 NewRoomPage/AgentConfigCard 同步走 gateway 选择。

Purpose: 用户必须在应用内能填入自己的 API key + baseUrl + 默认模型（SC#1 直接前置条件）。本 plan 让浏览器直连路径有 UI 配置面，并将 D-06 multi-key crypto 暴露给用户操作。
Output: /settings 完整 CRUD + 测试连接 + 设计 token 基座 + sidebar/router 接入 + agent 创建流改造。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-production-model-gateway/01-CONTEXT.md
@.planning/phases/01-production-model-gateway/01-UI-SPEC.md
@.planning/phases/01-production-model-gateway/01-01-PLAN.md
@.claude/skills/sketch-findings-councilkit/SKILL.md

@src/styles/globals.css
@tailwind.config.ts
@src/components/layout/Sidebar.tsx
@src/app/router.tsx
@src/components/ui/Button.tsx
@src/components/ui/Modal.tsx
@src/components/ui/Select.tsx
@src/components/ui/TextInput.tsx
@src/components/shared/EmptyState.tsx
@src/components/agent/AgentConfigCard.tsx
@src/app/pages/NewRoomPage.tsx
@src/lib/db.ts
@src/lib/crypto.ts
@src/models/gateway.ts
@src/services/gateway-adapters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 设计 token 扩展 + StatusPill 通用组件</name>
  <files>src/styles/globals.css, tailwind.config.ts, src/components/shared/StatusPill.tsx</files>
  <read_first>
    - src/styles/globals.css（current :root 含 bg/surface/fg/muted/accent/border 6 个变量；@layer base body 应用）
    - tailwind.config.ts（current extend.colors 含 bg/surface/fg/muted/accent/edge 6 个映射）
    - .planning/phases/01-production-model-gateway/01-UI-SPEC.md §Design System（5 个新 CSS 变量值 + surface-2 #1e2128；authorized spacing scale {4,8,12,16,20,24,28,32,48,64}；weights {400,600}）
    - .claude/skills/sketch-findings-councilkit/SKILL.md（surface-2 #1e2128 + agent 色系 + accent #6366f1 用法）
  </read_first>
  <action>
    src/styles/globals.css `:root` 块新增 5 个 CSS 变量：`--color-success:#22c55e`、`--color-warn:#d29922`、`--color-error:#f85149`、`--color-info:#58a6ff`、`--color-surface-2:#1e2128`（值与 UI-SPEC 一致，Phase 2 VERIFY-01 全局落地同一组值，不发散）。`@layer base` 不改动（success/warn/error/info 不进 body 默认）。

    tailwind.config.ts `theme.extend.colors` 增 5 项：`success:"var(--color-success)"`、`warn:"var(--color-warn)"`、`error:"var(--color-error)"`、`info:"var(--color-info)"`、`"surface-2":"var(--color-surface-2)"`。注：Tailwind 键 `surface-2` 含连字符需用引号字符串键名。

    新建 src/components/shared/StatusPill.tsx：props `{ tone: 'muted'|'info'|'success'|'error'|'warn'; text: string; className?: string }`。CSS 类按 tone 映射：muted → `bg-surface-2 text-muted border border-edge`；info → `bg-info/10 text-info border border-info`；success → `bg-success/10 text-success border border-success`；error → `bg-error/10 text-error border border-error`；warn → `bg-warn/10 text-warn border border-warn`。基础类 `inline-flex items-center rounded px-2 py-0.5 text-xs leading-snug`（UI-SPEC 12px label）。**不含 emoji/glyph**（颜色不作为唯一信号由调用方加 ⚠ 等前缀）。

    本 Task 不接igate 现有组件改色（避免 stale）。
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/biome check src/styles/globals.css tailwind.config.ts src/components/shared/StatusPill.tsx</automated>
    <automated>rg -c "color-success|color-surface-2" src/styles/globals.css | grep -q "$(printf '5\|[5-9]')" && echo "tokens present"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css 含全部 5 个新 CSS 变量名（--color-success/warn/error/info/surface-2）与对应 hex 值
    - tailwind.config.ts extend.colors 含 success/warn/error/info/surface-2 五个映射键
    - StatusPill 接受 5 个 tone 值，渲染 span 含 border + text-{tone} + bg-{tone}/10 类
    - tsc 0 错；biome 通过
  </acceptance_criteria>
  <done>设计 token 与 StatusPill 落地，TypeScript/lint 绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useGateways store + gateway-test helper + Settings 页 CRUD 组件</name>
  <files>src/stores/gateways.ts, src/lib/gateway-test.ts, src/components/gateway/GatewayCard.tsx, src/components/gateway/GatewayFormModal.tsx, src/components/gateway/TestConnectionButton.tsx, src/components/gateway/DeleteGatewayModal.tsx, src/app/pages/SettingsPage.tsx, tests/unit/settings-store.test.ts</files>
  <read_first>
    - src/lib/db.ts（P01 产出 addGateway/getGateway/listGateways/updateGateway/deleteGateway + db.gateways 表）
    - src/lib/crypto.ts（P01 产出 saveGatewayApiKey/loadGatewayApiKey/clearGatewayApiKey/gatewayKeyStorageId）
    - src/models/gateway.ts（P01 产出 Gateway interface + createGateway + validateGateway + GatewayType）
    - src/services/gateway-adapters.ts（P02 产出 anthropicAdapter/openaiCompatibleAdapter/normalizeBaseUrl）
    - src/components/ui/Button.tsx, Modal.tsx, Select.tsx, TextInput.tsx（既有组件库，Button variant='primary'|'ghost'，Modal 含 ESC 关闭）
    - src/components/shared/EmptyState.tsx（heading + hint 两段）
    - .planning/phases/01-production-model-gateway/01-UI-SPEC.md §Copywriting Contract（settings 页全部文案、agent modal 文案、错误 pill 文案、测试连接 pill 文案、5 字段 label/placeholder/help）+ §Interaction Flows /settings CRUD + §Component Inventory
    - .planning/phases/01-production-model-gateway/01-CONTEXT.md D-07 (apiKey 仅 localStorage), D-08 (测试连接最小 stream 请求)
  </read_first>
  <behavior>
    - Test: useGateways query 返回 db.gateways 全量（listGateways）；CRUD mutation 调用对应 db helper 并 invalidate query
    - Test: testGatewayConnection 调 anthropicAdapter（type=anthropic）或 openaiCompatibleAdapter（type=openai-compatible），生成 model=gateway.defaultModel、max_tokens=1、stream:true 的最小请求，for await 收到任何 string chunk → {ok:true}；遇到 GatewayError → {ok:false, error}（不抛错，封装结果）
    - Test: testGatewayConnection 在 loadGatewayApiKey(gateway.id) === null 时返回 {ok:false, error:{kind:'invalid_key', message:'未配置 API 密钥'}}（不调 fetch）
  </behavior>
  <action>
    新建 src/stores/gateways.ts：导出 `gatewayKeys = { list: ['gateways'] as const, detail: (id)=>['gateway', id] as const }`；`useGateways()` = useQuery({queryKey: gatewayKeys.list, queryFn: () => listGateways()})；`useCreateGateway()` = useMutation async (input: {name, type, baseUrl, defaultModel, apiKey: string}) → { const g = createGateway({name, type, baseUrl, defaultModel}); await addGateway(g); saveGatewayApiKey(g.id, input.apiKey); return g; } onSuccess invalidate gatewayKeys.list；`useUpdateGateway()` = useMutation async ({id, changes, apiKey?}) → { await updateGateway(id, changes); if (apiKey !== undefined && apiKey.length > 0) saveGatewayApiKey(id, apiKey); } onSuccess invalidate；`useDeleteGateway()` = useMutation async (id) → { await deleteGateway(id); clearGatewayApiKey(id); } onSuccess invalidate list + invalidate agent 查询（agent 字段未自清，仅触发 UI 刷新）。

    新建 src/lib/gateway-test.ts：导出 `async function testGatewayConnection(gateway: Gateway): Promise<{ok:true}|{ok:false, error:GatewayError}>`。流程：(1) const apiKey = loadGatewayApiKey(gateway.id)；if (!apiKey) return {ok:false, error:{kind:'invalid_key', message:'未配置 API 密钥'}}。(2) 选 adapter by gateway.type。(3) 构造 messages = [{role:'user', content:'ping'}]；for await chunk of adapter({baseUrl:gateway.baseUrl, apiKey, model:gateway.defaultModel, messages}) { if (typeof chunk === 'string' && chunk.length>0) return {ok:true}; if (typeof chunk !== 'string') return {ok:false, error:chunk}; }。(4) loop 结束未收到 string chunk（只收到 message_stop）→ return {ok:true}（已成功建立连接，max_tokens=1 可能无可见 delta）。catch 任何 throw → {ok:false, error:{kind:'network', message: err.message}}。

    新建 src/components/gateway/GatewayCard.tsx：props `{ gateway: Gateway; status: TestStatus; onEdit: ()=>void; onDelete: ()=>void; onTest: ()=>void }`。TestStatus type = 'idle'|'testing'|'success'|'failed-fatal'|'failed-other'。布局：rounded border-edge bg-surface px-4 py-3（UI-SPEC padding lg 16px）+ hover:border-accent。内容行：left = avatar（首字母圆 h-6 w-6 font-semibold）+ name (14px font-semibold) + type badge（StatusPill muted, text=gateway.type==='anthropic'?'Anthropic':'OpenAI 兼容'）+ baseUrl（12px font-mono text-muted，需引用 tailwind `font-mono`，未定义则用内联 style fontFamily 'ui-monospace,monospace'）+ defaultModel（12px text-muted）。right = TestConnectionButton + 编辑网关 (ghost text-sm) + 删除 (text-error hover 14px ghost)。整张卡 `<div role="button" tabIndex={0}` Enter 触发 onEdit（UI-SPEC keyboard）。

    新建 src/components/gateway/TestConnectionButton.tsx：props `{ status: TestStatus; onClick: ()=>void; failedKind?: GatewayErrorKind }`。状态映射（UI-SPEC pill/button copywriting）：
      idle → Button ghost text:'测试连接' onClick
      testing → Button ghost disabled text:'测试中…'
      success → Button disabled success-tinted bg-success/10 text-success border border-success text:'已连接'（不需 h-10 但 UI-SPEC 提示测试连接主操作需 h-10 —— 加 `h-10` class）
      failed-fatal → Button ghost error-tinted bg-error/10 text-error text:'重试测试' onClick
      failed-other → 同 failed-fatal 文案 '重试测试'
    全部使用既有 Button 组件 + className 扩展（不允许引入新 Button variant 类型）。

    新建 src/components/gateway/GatewayFormModal.tsx：props `{ open; mode: 'create'|'edit'; initial?: Gateway; onClose; onSubmit: (vals: {name, type, baseUrl, defaultModel, apiKey?: string})=>void }`。状态：useState 4 字段 + apiKey（编辑态空字符串表示不改）。字段渲染：TextInput name (label='名称' placeholder='例: Claude 主账号' help 行下小字 '仅本机显示。')、Select type (label='类型' options=[{value:'anthropic',label:'Anthropic (/v1/messages)'},{value:'openai-compatible',label:'OpenAI 兼容 (/v1/chat/completions，含 OpenAI/DeepSeek/Ollama/vLLM)'}])、TextInput baseUrl (label='Base URL' placeholder='例: https://api.anthropic.com' help '只填 host。代码按类型自动拼 /v1/messages 或 /v1/chat/completions。')、TextInput apiKey type='password' (label='API 密钥' placeholder='sk-…' help 'AES 加密后存于 localStorage，不会离开本机浏览器。' 编辑态 placeholder '••••••••' 默认空)、TextInput defaultModel (label='默认模型 ID' placeholder='例: claude-sonnet-4' help '新建 agent 时据此预填。可在 agent 编辑中覆盖。')。底部：取消 (Button ghost) + 保存网关 (Button primary h-10)。校验：4 字段非空（apiKey 编辑态可空；create 态必填）—— 不满足显示 form error 文案 '请填写名称、类型、base URL 与 API 密钥。'。Modal 用既有 Modal 组件；title = mode==='create'?'添加网关':'编辑网关'。

    新建 src/components/gateway/DeleteGatewayModal.tsx：props `{ open; gateway: Gateway|null; agentCount: number; onClose; onConfirm: ()=>void }`。布局：Modal title=`删除网关「${gateway?.name}」？` + body 文案 UI-SPEC `该网关下 ${agentCount} 个 agent 的 gatewayId 将被清空并标记为离线。密钥将从本机删除。此操作不可撤销。`（**注**：agent 字段通过 useDeleteGateway 不主动清空 —— P03 文案与实际行为对齐：onConfirm 后 agent.gatewayId 仍指向已删 gateway id，下次 runRound resolveGatewayAndKey 会 getGateway 返回 undefined，需 P04/dispatch 处理。本 Task 文案如实保留 UI-SPEC 原文，但 action 内 onConfirm 仅 deleteGateway+clearGatewayApiKey，不动 agent。）：底部 取消 (default focus) + 删除 (Button variant 改造或 inline class bg-error text-white)。Modal open 时 useEffect focus 取消按钮（ref.current?.focus()，UI-SPEC a11y D 部破坏安全选择）。ESC 走 Modal 内置。

    新建 src/app/pages/SettingsPage.tsx：用 useGateways() + 3 mutations。state: modalOpen(formModal), formMode, editing, deleteGateway target。layout: div.mx-auto.max-w-2xl.px-6.py-8 → h1 '设置' (text-xl font-semibold) → section: heading '模型网关' (text-base font-semibold) + sub '配置浏览器直连的模型 endpoint。密钥以 AES 加密存储在本机 localStorage，按 gateway 分别管理。' (text-sm text-muted) → 顶部右 + 添加网关 Button primary h-10 → 列表（gateways.length===0 ? EmptyState heading '还没有网关' body '添加一个网关以让浏览器直连模型端点…' cta '+ 添加网关' : gateway list .flex.flex-col.gap-2 含 GatewayCard × N）。每个 GatewayCard 接：onEdit=开 formModal mode='edit' initial=g、onDelete=开 DeleteGatewayModal target=g agentCount=useAgentCountByGateway(g.id)（小 helper or inline db.agents.filter）、onTest=useMutation 调 testGatewayConnection 更新本地 status state（per-gateway status map Record<id, TestStatus>）。状态初始 'idle'。

    tests/unit/settings-store.test.ts：mock db.gateways + crypto，验证 useGateways/useCreateGateway/useUpdateGateway/useDeleteGateway 行为（query 返回、mutation 调 helper + invalidate）；testGatewayConnection 3 路径（无 key、adapter 返 string、adapter 返 GatewayError）。
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/settings-store.test.ts</automated>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/biome check src/components/gateway src/app/pages/SettingsPage.tsx src/lib/gateway-test.ts src/stores/gateways.ts</automated>
  </verify>
  <acceptance_criteria>
    - useGateways query 返回 listGateways()；3 个 mutation 各自调对应 db helper + save/clear key + invalidate
    - testGatewayConnection 无 key 返 {ok:false, error:{kind:'invalid_key'}}；adapter 返 string 返 {ok:true}；adapter 返 GatewayError 返 {ok:false, error}
    - SettingsPage 列表渲染 gateway × N（含 name/type badge/baseUrl mono/defaultModel/3 actions）；空态走 EmptyState + CTA
    - GatewayFormModal 5 字段 label/placeholder/help 与 UI-SPEC copywriting 表完全一致；编辑态 apiKey placeholder '••••••••'
    - TestConnectionButton 5 状态（idle/testing/success/failed-fatal/failed-other）文案与 UI-SPEC 一致；testing+success disabled；success 绿 tint；failed 红 tint
    - DeleteGatewayModal 标题含 gateway.name；body 含 N（agentCount）与「此操作不可撤销」；取消按钮默认 focus；ESC 关闭（Modal 内置）
    - tsc 0 错，biome 通过
  </acceptance_criteria>
  <done>SettingsPage 完整 CRUD + 测试连接 + store + helper 落地；5 状态按钮 + 5 字段表单 + 删除确认全部按 UI-SPEC 文案；单测绿</done>
</task>

<task type="auto">
  <name>Task 3: Sidebar 第 4 项 + router /settings + NewRoomPage gateway select + AgentConfigCard subtitle</name>
  <files>src/components/layout/Sidebar.tsx, src/app/router.tsx, src/app/pages/NewRoomPage.tsx, src/components/agent/AgentConfigCard.tsx</files>
  <read_first>
    - src/components/layout/Sidebar.tsx（current nav 主页/新建房间/模板 + footer 文案 'local-first · 多 agent 决策'；NavLink className roundedLink fn）
    - src/app/router.tsx（current 4 routes 含 templatesPage 占位 EmptyState）
    - src/app/pages/NewRoomPage.tsx（current ModelType select + draftModel state + createAgent({model: draftModel, role, color})）
    - src/components/agent/AgentConfigCard.tsx（current subtitle `<p className="text-xs text-muted">{agent.model}</p>`）
    - src/stores/gateways.ts（Task 2 产出 useGateways）
    - .planning/phases/01-production-model-gateway/01-UI-SPEC.md §Sidebar 4th entry（footer 位置 + border-edge 分隔 + 设置 NavLink + active bg-accent/20 text-fg）+ §Agent creation flow change（modal title 添加 agent + 网关 Select + 模型 ID TextInput 预填 defaultModel + 无 gateway warn 通知 + 前往设置 链接 + 确认添加 disabled when role 空或无 gateway）
    - .planning/phases/01-production-model-gateway/01-CONTEXT.md D-02 (Agent.gatewayId+model) D-05 (sidebar 设置)
  </read_first>
  <action>
    src/components/layout/Sidebar.tsx：在现有 `<nav>` 之后、`<p>` footer 之前插入 `<div className="border-t border-edge my-2" />` + `<div className="px-2"><NavLink to="/settings" className={({isActive})=>roundedLink(isActive)}>设置</NavLink></div>`。roundedLink 已存在不重写。footer `<p>` 保留。NavLink active 类沿用现有 bg-accent/20 text-fg。

    src/app/router.tsx：import SettingsPage from '@/app/pages/SettingsPage'；在 templatesPage route 之后加 `{ path: "/settings", element: withShell(<SettingsPage />) }`。

    src/app/pages/NewRoomPage.tsx：删除 MODEL_OPTIONS 常量与 draftModel state（ModelType）。引入 useGateways() 取 gateways。state 新增 `draftGatewayId: string`、`draftModel: string`（不再 ModelType，改 string）。Modal 内字段：保留 role TextInput；删除 ModelType Select；新增 Select 网关 `<Select label="网关" options={gateways.map(g=>({value:g.id, label:g.name}))} value={draftGatewayId} onChange={(e)=>{ setDraftGatewayId(e.target.value); const g = gateways.find(x=>x.id===e.target.value); if (g) setDraftModel(g.defaultModel); }} />`；新增 TextInput 模型 ID `<TextInput label="模型 ID" value={draftModel} onChange={(e)=>setDraftModel(e.target.value)} placeholder="例: claude-sonnet-4" />`。当 `gateways.length===0`：替换 gateway Select + 模型 TextInput 为 warn 通知块 `<div className="rounded border border-warn bg-warn/10 p-4 text-fg">` 含文案 UI-SPEC 「尚未配置任何网关。前往「设置」添加一个网关后再创建 agent。」 + 「前往设置」按钮 onClick navigate('/settings')。确认添加按钮 disabled 条件改为 `draftRole.trim().length===0 || !draftGatewayId`。createAgent 调用从 `{model: draftModel, ...}` 改为 `{gatewayId: draftGatewayId, model: draftModel, role, color}`（P02 已改 createAgent 签名）。validation：若 gateways.length===0，整个 添加 agent Button 应仍可点但 Modal 内显示 notice（不预 disable 主按钮）。

    src/components/agent/AgentConfigCard.tsx：props 加 `gatewayName?: string`；subtitle 行从 `{agent.model}` 改为 `{gatewayName ? `${gatewayName} · ${agent.model}` : agent.model}`。NewRoomPage 渲染处传 `gatewayName={gateways.find(g=>g.id===a.gatewayId)?.name}`。

    无新单测（本 Task 是 UI wiring，依赖 P02 createAgent 签名，验证靠 tsc + biome + 浏览器手测 P05）。
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/biome check src/components/layout/Sidebar.tsx src/app/router.tsx src/app/pages/NewRoomPage.tsx src/components/agent/AgentConfigCard.tsx</automated>
    <automated>rg -c 'path: "/settings"' src/app/router.tsx | grep -q 1 && echo "settings route present"</automated>
    <automated>rg -c 'to="/settings"' src/components/layout/Sidebar.tsx | grep -q 1 && echo "sidebar entry present"</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar 含 `<NavLink to="/settings">设置</NavLink>` 置于 border-edge 分隔后的 footer 区，不与 主页/新建房间/模板 同 nav 块
    - router 含 `{ path: "/settings", element: withShell(<SettingsPage />) }`
    - NewRoomPage Modal 含「网关」Select（options=gateway.name）+「模型 ID」TextInput（onChange gateway 切换时同步 defaultModel）；gateways.length===0 显示 warn 通知块 + 前往设置 链接；确认添加 disabled 当 role 空 OR 无 gateway 选中
    - AgentConfigCard subtitle 显示 `${gatewayName} · ${agent.model}`（gatewayName 缺失时 fallback 仅 model）
    - createAgent 调用传 gatewayId: draftGatewayId
    - tsc 0 错，biome 通过
  </acceptance_criteria>
  <done>Sidebar/router/NewRoomPage/AgentConfigCard 全部接入 gateway，navigation & agent 创建流走新模型字段</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings page UI ↔ localStorage cipher | apiKey 通过此边界写入；表单 input 是明文（用户手输），落盘前 AES 加密 |
| settings page UI ↔ Dexie gateways | gateway 元数据 CRUD 跨此边界；不含 key |
| test connection button ↔ cross-origin endpoint | 「测试连接」触发真实 fetch 直连上游；key 在 header 跨域 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | apiKey 在 form input 显示明文 | medium | mitigate | GatewayFormModal apiKey 字段 type='password' 掩码；编辑态 placeholder '••••••••' 而非回填明文；StatusPill 不渲染 key；列表 GatewayCard 不显示 key 字段 |
| T-03-02 | Information Disclosure | baseUrl/defaultModel 在 UI 旁路泄露 | low | accept | baseUrl/defaultModel 是用户自配非密；Dexie 表可能含 endpoint URL，本地单用户可接受 |
| T-03-03 | Tampering | DeleteGatewayModal 不动 agent.gatewayId → 悬空引用 | medium | mitigate | 文案如实告知「该网关下 N 个 agent 的 gatewayId 将被清空」（UI-SPEC 文案）；实际 onConfirm 仅 delete gateway+key，agent.gatewayId 由 P04 dispatch 处理 fallback（resolveGatewayAndKey 返 undefined → invalid_key 离线）。本 Task 通过 P04 dispatch 容错兜底而非主动清空字段 |
| T-03-04 | Spoofing | testGatewayConnection 调真实外部 endpoint 暴露 key | medium | accept | 用户主动配置 endpoint（D-07 自担）；测试连接是用户主动触发，结果仅本地 UI 显示；不构成被动泄露面 |
| T-03-05 | Denial of Service | 用户频繁点测试连接消耗配额 | low | accept | 单次最小请求 max_tokens=1；按钮 testing 状态 disabled；UI-SPEC 4 态防重复；不引入节流避免过度设计 |
| T-03-SC | Tampering | 无 npm 安装 | low | accept | 无新依赖 |
</threat_model>

<verification>
- ./node_modules/.bin/vitest run tests/unit/settings-store.test.ts 全绿
- ./node_modules/.bin/tsc --noEmit 0 错
- ./node_modules/.bin/biome check src 0 错
- 浏览器手测（P05 实跑）：/settings 渲染、添加 gateway、测试连接、删除、sidebar 设置入口可点、新建房间 Modal gateway select
</verification>

<success_criteria>
- /settings 路由可达，gateway CRUD 完整可用（D-05/D-07）
- 测试连接 4 态切换正确（D-08）
- 设计 token 5 色落地（D-06 共用基础，与 P04 错误呈现一致）
- sidebar 第 4 项「设置」（D-05）
- 新建房间 Modal 走 gateway select + model TextInput（D-02 配套）
- apiKey 全程不显示明文
- 全部 typecheck/lint/unit 绿
</success_criteria>

<output>
Create `.planning/phases/01-production-model-gateway/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- **Design tokens** (src/styles/globals.css + tailwind.config.ts): --color-success/warn/error/info/surface-2 + Tailwind color 映射
- **Reusable component** (src/components/shared/StatusPill.tsx): tone + text pill
- **Gateway test helper** (src/lib/gateway-test.ts): `testGatewayConnection(gateway)`
- **Gateway UI components** (src/components/gateway/*): GatewayCard, GatewayFormModal, TestConnectionButton, DeleteGatewayModal
- **Settings page** (src/app/pages/SettingsPage.tsx): gateway 列表 + CRUD orchestration
- **Gateway store** (src/stores/gateways.ts): useGateways, useCreateGateway, useUpdateGateway, useDeleteGateway + gatewayKeys
- **Sidebar entry** (src/components/layout/Sidebar.tsx): footer 设置 NavLink
- **Router** (src/app/router.tsx): /settings route
- **Agent creation flow** (src/app/pages/NewRoomPage.tsx): gateway Select + 模型 ID TextInput + empty notice
- **Agent card** (src/components/agent/AgentConfigCard.tsx): subtitle `{gatewayName} · {agent.model}`
- **Tests** (tests/unit/settings-store.test.ts): store + testGatewayConnection 3 路径
