---
phase: 01-production-model-gateway
plan: 05
plan_id: 01-05
type: execute
wave: 5
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - src/main.tsx
  - vite.config.ts
  - .env.example
  - scripts/model-proxy.mjs
  - README.md
  - src/lib/stream.ts
files_deleted: []
autonomous: false
requirements: [GW-01]
user_setup:
  - service: anthropic
    why: "D-13 实跑验证浏览器直连 Anthropic CORS（anthropic-dangerous-direct-browser-access:true）+ SC#2 E2E 真实讨论流"
    env_vars: []
    dashboard_config:
      - task: "用户自备有效 Anthropic API key（sk-ant-…）手输到 /settings gateway，不留 env"
        location: "Anthropic Console → API Keys"
  - service: openai-compatible
    why: "验证 openai-compatible adapter 浏览器直连 + 至少一个非 Anthropic provider 端到端"
    env_vars: []
    dashboard_config:
      - task: "用户自备有效 OpenAI 或 DeepSeek API key 手输到 /settings 第二个 gateway"
        location: "OpenAI Platform → API keys / DeepSeek platform"

must_haves:
  truths:
    - "main.tsx 启动时调用 `runStartupMigration()`（P01 产出），错误 swallow + console.warn，不阻塞渲染 —— D-03 启动迁移实际接入"
    - "vite.config.ts 中 `server.proxy /api/claude` 与 `MODEL_PROXY_URL` env 保留但标注「dev-only harmless，生产 build 不依赖」—— 不删（避免破坏 dev 临时验证）但显式声明非生产路径"
    - ".env.example 改写：VITE_CLAUDE_API_KEY / VITE_OPENAI_API_KEY / VITE_DEEPSEEK_API_KEY / VITE_*_BASE_URL 全部标为「dev-only optional；生产从 /settings localStorage 读 key，不读 env」；MODEL_PROXY_PORT/MODEL_PROXY_URL 标 dev-only"
    - "scripts/model-proxy.mjs 顶部注释加 'Note (Phase 1): 生产路径不再依赖此 proxy（用户在 /settings 自持 key 浏览器直连）；保留仅供 dev 验证或临时 mock。' 不删除"
    - "README.md 生产运行说明：用户在 /settings 配置 gateway (name+type+baseUrl+key+defaultModel) 即可，无需启动 model-proxy.mjs"
    - "人类实跑验证：/settings 填 Anthropic gateway (type='anthropic', baseUrl='https://api.anthropic.com', defaultModel='claude-sonnet-4-20250514') + 真实 sk-ant key，点「测试连接」→ 状态 success「已连接」—— 验证 D-13 anthropic-dangerous-direct-browser-access:true 头在浏览器 fetch 直连生效（CORS 通过）"
    - "人类实跑验证：/settings 填 OpenAI 兼容 gateway (type='openai-compatible', baseUrl='https://api.openai.com', defaultModel='gpt-4o-mini') + 真实 key，点「测试连接」→ 状态 success「已连接」"
    - "人类实跑验证 (SC#2)：/rooms/new 建 room + 加 2 agent (一 Claude 一 OpenAI 兼容) → 发起讨论 → agent 流式输出真实模型响应 → 自动生成 summary → UserInputBar 追问 → 第二轮 agent 看到 summary + 用户输入继续发言"
    - "人类实跑验证 (SC#3)：停止 model-proxy.mjs：8788 进程，仅 `pnpm dev`（仅 vite）启动；浏览器 DevTools Network 面板无 /api/claude 请求；上述 E2E 流程仍可跑通"
    - "人类实跑验证 (SC#4)：故意把 Anthropic gateway key 改错 → 起 round → agent 看到 inline 红「⚠ 密钥无效，已离线」+ 顶部 banner「网关「{name}」已离线：密钥无效…」；其他 gateway 的 agent 正常发言；全 agent offline 时 banner「本轮无有效发言，未生成总结。」且 round 无 summary"
    - "代码层 typecheck/lint/build 全绿：tsc --noEmit + biome check src + vite build 成功（vite build 不依赖 cld/proxy/claude-binary）"
    - "src/lib/stream.ts 中 collectText 函数已删除（P02 标 @deprecated 后的孤儿清理）；rg 'collectText' src/ 零命中"
  artifacts:
    - "src/main.tsx — 接入 runStartupMigration"
    - "vite.config.ts — proxy 注释 + dev-only 标注"
    - ".env.example — VITE_* 全部标 dev-only optional"
    - "scripts/model-proxy.mjs — 头部注释 Phase 1 说明"
    - "README.md — 生产运行说明段落"
    - "src/lib/stream.ts — 删除 collectText 函数（P02 孤儿清理）"
  key_links:
    - "main.tsx → runStartupMigration (P01) → migrateLegacyAgentsToGateways (D-03)"
    - "/settings Anthropic gateway → anthropicAdapter (P02) → fetch anthropic.com with anthropic-dangerous-direct-browser-access:true → SSE → 真实模型响应 (SC#1)"
    - "/settings → /rooms/new → runRound → anthropicAdapter/openaiCompatibleAdapter → 真实模型流式 (SC#2)"
    - "E2E 验证不依赖 model-proxy.mjs（SC#3）"
---

<objective>
Phase 1 收官验证：把 P01 启动迁移接入应用入口，清理 dev proxy 路径文档（SC#3），并完成 D-13 Anthropic CORS 浏览器直连 + 全部 4 条 ROADMAP 成功标准的 human-verify 实跑证明。

Purpose: 前 4 plans 是自动化实现；本 plan 通过真实浏览器 + 真实 API key 锁死「浏览器直达、无 proxy 进程」的生产路径假设（D-13 CORS 头是 P02 仅就位未验证的关键假设），并清理 dev 旁路声明。Phase 2 VERIFY-03 直接依赖本 plan 的真实 API E2E 能跑通。
Output: main.tsx 接入迁移 + dev proxy 清理注释 + .env.example/README 更新 + 人类实跑 E2E 证据（不产 SUMMARY 之外的代码产出，主要产出是验证完成）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-production-model-gateway/01-CONTEXT.md
@.planning/phases/01-production-model-gateway/01-UI-SPEC.md
@.planning/phases/01-production-model-gateway/01-01-PLAN.md
@.planning/phases/01-production-model-gateway/01-02-PLAN.md
@.planning/phases/01-production-model-gateway/01-03-PLAN.md
@.planning/phases/01-production-model-gateway/01-04-PLAN.md

@src/main.tsx
@vite.config.ts
@scripts/model-proxy.mjs
@.env.example
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 接入启动迁移 + dev proxy 清理 + 文档更新</name>
  <files>src/main.tsx, vite.config.ts, .env.example, scripts/model-proxy.mjs, README.md, src/lib/stream.ts</files>
  <read_first>
    - src/main.tsx（current createRoot render App+Router；启动注入点）
    - src/lib/gateway-migrate.ts（P01 产出 runStartupMigration swallowing 错误 console.warn）
    - vite.config.ts（current server.proxy /api/claude + MODEL_PROXY_URL env fallback 8788；dev 注释）
    - .env.example（current VITE_CLAUDE_API_KEY / VITE_CLAUDE_BASE_URL / VITE_OPENAI_API_KEY / VITE_OPENAI_BASE_URL / VITE_DEEPSEEK_API_KEY / VITE_DEEPSEEK_BASE_URL / MODEL_PROXY_PORT / MODEL_PROXY_URL）
    - scripts/model-proxy.mjs（current 头部注释「DEV 验证用」「生产环境不适用」）
    - README.md（current 项目说明，若不存在则创建）
    - src/lib/stream.ts（P02 Task 2 已标 collectText `@deprecated Phase 1`，dispatchMessage 不再调用；本 Task 删 collectText）
    - .planning/phases/01-production-model-gateway/01-CONTEXT.md D-03 (启动迁移接入), D-13 (浏览器直连)
    - .planning/ROADMAP.md §Phase 1 SC#3 (干净检出 + model-proxy 不在生产路径)
  </read_first>
  <action>
    src/main.tsx：在 `createRoot(...).render(...)` 之前调 `runStartupMigration()`（不 await —— fire-and-forget 异步，避免阻塞首屏；runStartupMigration 内部已 swallowing 错误 console.warn）。导入从 '@/lib/gateway-migrate'。render 同步发起。

    vite.config.ts：`server.proxy['/api/claude']` 块上方注释从「CR1 模型路径(dev): …生产需另行方案」改为「Phase 1: 此 dev proxy 仅 dev 模式语法保留，生产路径已切到 /settings gateway 浏览器直连（P02 anthropicAdapter/openaiCompatibleAdapter + D-13 CORS header）。proxy 不删除以免破坏 dev 临时 mock 验证。」不删 proxy 字段。

    .env.example：在每个 VITE_*_API_KEY 与 VITE_*_BASE_URL 行前加注释 `# dev-only optional (Phase 1 前)。Production 不读 env；用户在 /settings 配置 gateway + AES 加密 key 存 localStorage。`。MODEL_PROXY_PORT/MODEL_PROXY_URL 行前加 `# dev-only (model-proxy.mjs)；production 不需要。`。整体文件头加 Phase 1 说明。

    scripts/model-proxy.mjs：文件首行注释块追加 `// Note (Phase 1 Production Gateway, 2026-06-25): 生产路径不再依赖此 proxy。
    // 用户在 /settings 自持 key 浏览器直连（P02 adapter + D-13 Anthropic CORS header。
    // 保留此脚本仅供 dev 临时验证或本地 mock 场景。`。不删文件。

    README.md：新增 ## Production Setup 段（若文件不存在则创建并写最小 Project 介绍 + 此段）：
    ```
    ## Production Setup (Phase 1)

    1. `pnpm dev` 启动 vite（无需启动 scripts/model-proxy.mjs）
    2. 浏览器打开 /settings → 「+ 添加网关」→ 填 name / type (Anthropic | OpenAI 兼容) / Base URL (host 如 https://api.anthropic.com) / API 密钥 / 默认模型 ID
    3. 点「测试连接」验证 key + CORS 直连
    4. /rooms/new 创建房间 + 添加 agent（选 gateway + 模型 ID）→ 发起讨论

    API key 以 AES 加密存于浏览器 localStorage，按 gateway 分别管理，不出本机。
    ```

    src/lib/stream.ts：rg -n 'collectText' src/ 确认已零引用（P02 Task 2 标 @deprecated 后 dispatchMessage 不再调用，仅 collectText 自身定义在 stream.ts）。零引用 → 删除 collectText 函数定义 + export。保留 streamDeltas / StreamChunk / parseChunk 不动。再跑 tsc --noEmit 确认无下游破坏。
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/biome check src/main.tsx src/lib/stream.ts</automated>
    <automated>./node_modules/.bin/vite build</automated>
    <automated>! rg -n 'collectText' src/ && echo "collecttext removed"</automated>
    <automated>rg -c 'runStartupMigration' src/main.tsx | grep -q 1 && echo "migration wired"</automated>
    <automated>rg -c 'Phase 1: 此 dev proxy' vite.config.ts | grep -q 1 && echo "proxy annotated"</automated>
  </verify>
  <acceptance_criteria>
    - main.tsx 含 runStartupMigration() 调用（同步发起，不 await）
    - vite build 成功（不报 cld/proxy 相关错；pure client bundle 无 /api/claude 调用代码路径进 production chunk —— 注 vite.config server 仅 dev，build 不含）
    - collectText 已从 src/lib/stream.ts 移除；rg 'collectText' src/ 零命中（rip 任一命中即失败）
    - .env.example 全部 VITE_* 与 MODEL_PROXY_* 行含 dev-only 标注
    - scripts/model-proxy.mjs 头注释含 Phase 1 生产不依赖说明
    - README.md 含 Production Setup 段含 4 步
    - tsc 0 错，biome 通过
  </acceptance_criteria>
  <done>启动迁移接入 + dev proxy 路径清文档 + .env.example/README/scripts 注释更新 + collectText 孤儿清理 + vite build 绿</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-13 Anthropic CORS + SC#1/SC#2/SC#3/SC#4 人类实跑验证</name>
  <files>(verification only — no source changes; produces 01-05-SUMMARY.md evidence)</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 1 Success Criteria (4 条) — 全部需本 Task 实跑证明
    - .planning/phases/01-production-model-gateway/01-CONTEXT.md D-13 (Anthropic CORS anthropic-dangerous-direct-browser-access:true 头实跑验证)
    - .planning/phases/01-production-model-gateway/01-UI-SPEC.md §Copywriting Contract §Gateway error presentation (实跑对照文案) + §Test-connection status pill (实跑对照 pill 文案)
    - src/services/gateway-adapters.ts（P02 adapter 实现，验证 anthropic-dangerous-direct-browser-access:true header 在 fetch 中实际发出）
  </read_first>
  <action>checkpoint:human-verify task. 本 task 无源码改动、无自动化 verify —— 由人类在真实浏览器 + 真实 API key 下完成 7 步实跑验证（具体步骤见 &lt;how-to-verify&gt;；本 task 已构建的内容见 &lt;what-built&gt;；通过判据见 &lt;acceptance_criteria&gt;；用户回信号见 &lt;resume-signal&gt;）。验证重点为 D-13 anthropic-dangerous-direct-browser-access:true 头浏览器直连 Anthropic CORS 实际通过、SC#2 完整 E2E 真实模型输出、SC#3 干净检出无 /api/claude 调用、SC#4 5 类错误文案与 UI-SPEC copywriting 字面一致。</action>
  <what-built>
    P01-P04 自动化产出的完整 Production Model Gateway：通用 gateway 列表（Dexie gateways）+ AES 多 key crypto + 占位迁移 + Agent {gatewayId, model} 重构 + 双 adapter (anthropic/openai-compatible) + GatewayError 5 类 + runRound 致命扩散/全离线跳总结 + /settings CRUD + 测试连接 + ErrorBanner + MessageBubble inline。本 Task 验证以下场景在真实浏览器 + 真实 API key 下行为正确。
  </what-built>
  <how-to-verify>
    **前置准备（用户）：**
    - 拥有有效 Anthropic API key（sk-ant-…）和 OpenAI 或 DeepSeek API key 之一
    - 停止任何已在 :8788 跑的 model-proxy.mjs：`pkill -f model-proxy.mjs` 或 `lsof -ti:8788 | xargs kill`
    - 在项目根 `pnpm dev` 启动（仅 vite，不启动 proxy）

    **步骤 1 — 启动迁移接入验证（D-03）：**
    - 浏览器开 http://localhost:5173
    - DevTools Console 看无报错；若有旧 dev 数据（IndexedDB councilkit 中 agents 表含 model:'claude' 等标签），刷新后 DevTools Application → IndexedDB → councilkit → gateways 表应含 Claude/OpenAI/DeepSeek 占位（无 key），agents 表中对应记录的 model 字段已变 'claude-sonnet-4'/'gpt-4o'/'deepseek-chat'，且有 gatewayId 字段
    - 若是干净 DB（无旧 agent），gateways 表为空，无报错即可

    **步骤 2 — SC#1 + D-13 Anthropic CORS 直连验证：**
    - 导航 /settings（sidebar 底部「设置」）
    - 点「+ 添加网关」→ 填：名称 `Claude 主账号` / 类型 `Anthropic` / Base URL `https://api.anthropic.com` / API 密钥 `<真实 sk-ant-...>` / 默认模型 ID `claude-sonnet-4-20250514` → 「保存网关」
    - DevTools Network 面板开启
    - 点该 gateway 卡的「测试连接」按钮
    - 期望：
      - 按钮 transient 显示「测试中…」disabled
      - Network 出现 `POST https://api.anthropic.com/v1/messages` 请求 → 200 OK
      - 请求 Headers 含 `x-api-key`、`anthropic-version: 2023-06-01`、`anthropic-dangerous-direct-browser-access: true`（这是 D-13 关键验证点！）
      - 无 CORS 错误（无 `Access-Control-Allow-Origin` 报错）
      - 按钮变「已连接」+ success 绿 tint
      - 状态 pill 显示「已连接」绿色
    - 若 CORS 报错或返回 401/403：失败 → 检查 key 与 anthropic-dangerous-direct-browser-access header 实际是否在请求中（D-13 验证不通过则需回 P02 修）

    **步骤 3 — SC#1 OpenAI 兼容验证：**
    - 再「+ 添加网关」→ 名称 `OpenAI` / 类型 `OpenAI 兼容` / Base URL `https://api.openai.com` / API 密钥 `<真实 sk-...>` / 默认模型 ID `gpt-4o-mini` → 保存
    - 点「测试连接」→ 期望「已连接」+ Network `POST https://api.openai.com/v1/chat/completions` 200 + `Authorization: Bearer sk-...`

    **步骤 4 — SC#2 完整 E2E 真实讨论流：**
    - 导航 /rooms/new
    - 话题：`Phase 1 网关方案评审`
    - 「+ 添加 agent」Modal：角色 `产品经理` → 网关选 `Claude 主账号` → 模型 ID 自动填 `claude-sonnet-4-20250514` → 「确认添加」
    - 再加 agent：角色 `架构师` → 网关 `OpenAI` → 模型 `gpt-4o-mini` → 确认
    - 「创建并进入」
    - 房间页 → 「发起讨论」
    - 期望：
      - 两个 agent 依次 typing（drafting 流式文字逐字出现）
      - 真实模型响应内容（不是空字符串）
      - 自动生成 summary 在 SummaryBlock 显示
      - 无任何 agent offline
    - UserInputBar 输入「请补充安全考虑」→ 提交 → 「开始新一轮」
    - 期望：第二轮 agent 看到 summary + 用户消息后继续发言

    **步骤 5 — SC#3 干净检出无 proxy 验证：**
    - DevTools Network 面板过滤 `/api/claude` → 应为空（生产路径无 /api/claude 调用）
    - 全程无 model-proxy.mjs 进程：`lsof -i:8788` 应无输出
    - 步骤 4 的 E2E 在此前提下完整跑通

    **步骤 6 — SC#4 错误处理验证：**
    - /settings 编辑 Claude 主账号，把 API 密钥改成 `sk-ant-INVALID` → 保存
    - /rooms/new 建 2 agent 都选 Claude 主账号 → 发起讨论
    - 期望：
      - 第一个 agent inline 红「⚠ 密钥无效，已离线」+ body「网关 Claude 主账号 的 API 密钥被拒绝。请在「设置」更新密钥。」
      - 第二个 agent 不调请求，inline 红「⚠ 网关已离线」+ body「网关 Claude 主账号 已被标记离线，本轮跳过该 agent。」
      - 顶部 ErrorBanner 红色 role=alert：「网关「Claude 主账号」已离线：密钥无效。该网关下 2 个 agent 本轮已跳过。」
      - 无 summary 生成，额外 banner 黄色：「本轮无有效发言，未生成总结。」
    - /rooms/new 再建 1 agent Claude（错 key）+ 1 agent OpenAI（对 key）→ 发起讨论
    - 期望：Claude agent 红 inline + OpenAI agent 正常发言 → summary 基于 OpenAI 发言生成；顶部 banner 红（致命）+ 黄（recoverable 提示，但本场景只有 fatal，可仅红）
    - /settings 把 Claude key 改回正确 → 测试连接「已连接」→ 重开一轮 → 全部正常

    **步骤 7 — 测试连接 + 删除流程：**
    - /settings 任意 gateway「删除」→ 确认 Modal 显示正确 gateway 名 + 「N 个 agent 的 gatewayId 将被清空」+ 「此操作不可撤销」+ 取消默认 focus + ESC 关闭
    - 确认删除 → gateway 从列表消失 + DevTools Application localStorage 中 `councilkit.gateways.<id>.enc` 被清除

    **验证通过判据：** 全部 7 步符合预期，特别是步骤 2 (D-13 Anthropic CORS 200 + 已连接)、步骤 4 (SC#2 E2E)、步骤 5 (SC#3 无 /api/claude)、步骤 6 (SC#4 错误呈现)。
  </how-to-verify>
  <verify><human-check>真实浏览器 + 真实 API key 下完成 how-to-verify 7 步实跑；通过判据见 acceptance_criteria；用户输入 approved 触发 resume-signal。无需自动化命令。</human-check></verify>
  <resume-signal>输入 "approved" 表示全部 7 步验证通过；或描述具体失败步骤与 DevTools Network/Console 输出，便于回退到对应 P02-P04 plan 修正</resume-signal>
  <acceptance_criteria>
    - 步骤 1：启动迁移无报错（旧数据迁移或干净 DB 均 OK）
    - 步骤 2：Anthropic 测试连接 200 + 已连接；请求头含 anthropic-dangerous-direct-browser-access:true（D-13 验证通过的关键证据）
    - 步骤 3：OpenAI 兼容测试连接 200 + 已连接
    - 步骤 4：2 agent 真实流式发言 + summary 出现 + 追问第二轮可见（SC#2 通过）
    - 步骤 5：Network 无 /api/claude；8788 端口无进程；E2E 仍跑通（SC#3 通过）
    - 步骤 6：5 类错误文案与 UI-SPEC 一致；致命扩散正确；全离线跳总结正确（SC#4 通过）
    - 步骤 7：删除 gateway 清 localStorage key + Modal 文案与 a11y 正确
    - 全程无 CORS 错误（除故意填错 key 触发的 401/403）
    - 若步骤 2 D-13 验证不通过（CORS 错或 header 未生效），返回详情触发 P02 回修
  </acceptance_criteria>
  <done>4 条 ROADMAP SC + D-13 CORS + D-03 迁移接入全部由真实浏览器 + 真实 API key 实跑验证通过，证据进入 SUMMARY</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → cross-origin model endpoint (real) | 真实 API key 经此边界直连第三方；P02 adapter 已就位 D-13 header，本 plan 实跑验证 |
| localStorage → startTrace | runStartupMigration 在 main.tsx 启动触发迁移读写 localStorage + Dexie |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 真实 Anthropic/OpenAI key 经浏览器 fetch 暴露给第三方 endpoint | medium | accept | 用户主动配置 endpoint 自担（D-07 本地单用户边界）；HTTPS 保护传输；key 不进 git/log/网络日志。CORS 直连是 D-13 显式选择 |
| T-05-02 | Spoofing | 用户误填非 https baseUrl 暴露 key 给 MITM | medium | mitigate | P03 GatewayFormModal baseUrl 字段验证 `^https?://` —— 仅允许 http(s)，但未强制 https；本 plan 在 README Production Setup 强调「推荐 https」；localhost 自定义端点（Ollama 等）用 http 合理不强禁 |
| T-05-03 | Tampering | runStartupMigration fire-and-forget 失败被 swallow | low | mitigate | runStartupMigration 内部 try/catch + console.warn（P01 已落地）；首屏渲染不阻塞；用户在 DevTools console 可见 warn 不静默 |
| T-05-04 | Information Disclosure | DevTools Network 面板可见 Authorization header 含 key | low | accept | 本地开发者工具是用户自持；用户测试完应关闭/清理；不构成被动泄露面 |
| T-05-05 | Denial of Service | 用户实跑测试连接多 gateway 触发上游限流 | low | accept | 测试连接 max_tokens=1 最小成本；按钮 disabled 防重复；不引入跨 gateway 节流 |
| T-05-SC | Tampering | 无 npm 安装 | low | accept | 无新依赖；P05 仅注释 + 启动接线 |
</threat_model>

<verification>
- ./node_modules/.bin/tsc --noEmit 0 错
- ./node_modules/.bin/biome check src 0 错
- ./node_modules/.bin/vite build 成功（pure client bundle 不含 model-proxy 路径）
- 人类实跑 7 步全 approved
</verification>

<success_criteria>
- 启动迁移接入 main.tsx（D-03 闭环）
- dev proxy / model-proxy.mjs / .env.example 路径声明清理（SC#3 文档侧）
- 真实浏览器 + 真实 API key 跑通：Anthropic CORS 直连（D-13 验证通过）+ OpenAI 兼容 + 完整 E2E + 干净检出 + 错误处理（SC#1/2/3/4 全部实跑证明）
- 全部 4 条 ROADMAP Phase 1 Success Criteria 实跑通过
- 全部 13 条 D-01..D-13 决策已落地并经验证
</success_criteria>

<output>
Create `.planning/phases/01-production-model-gateway/01-05-SUMMARY.md` when done — SUMMARY 必须包含人类实跑 7 步通过证据（截图或 Network/Console 输出要点），作为 Phase 2 VERIFY-03 真实 API E2E 的前置依赖
</output>

## Artifacts this phase produces

- **App entry** (src/main.tsx): runStartupMigration() fire-and-forget 接入
- **Dev path docs** (vite.config.ts): proxy 块 Phase 1 注释
- **Env template** (.env.example): 全部 VITE_*/MODEL_PROXY_* 标 dev-only
- **Dev proxy script** (scripts/model-proxy.mjs): 头部 Phase 1 说明注释（不删）
- **README** (README.md): Production Setup 4 步说明
- **Stream cleanup** (src/lib/stream.ts): 删除 collectText 函数（P02 marked @deprecated 的孤儿函数，dispatchMessage 不再调用）
- **Verification evidence** (01-05-SUMMARY.md): 7 步人类实跑通过证据，作为 Phase 2 VERIFY-03 输入
